-- Monitor configuration
-- Single monitor, auto-detect preferred resolution and scale (Lua API)

hl.monitor({
    output   = "",
    mode     = "preferred",
    position = "auto",
    scale    = "auto",
})
