# Hyprpaper skeleton config (Story 2.8) — FLAT STATIC.
# The default install path is baked in ($XDG_DATA_HOME/dotfiles/ defaults to
# ~/.local/share/dotfiles/). This file is correct ONLY for the default install;
# a non-default install_dir is re-baked by Phase 2. Do not template this file.
preload = ~/.local/share/dotfiles/wallpapers/default.png
wallpaper = ,~/.local/share/dotfiles/wallpapers/default.png
