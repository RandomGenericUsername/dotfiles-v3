---
topic: dotfiles-repo-v3 Phase 4 reconciliation-engine decision handoff
updated: 2026-09-11T10:22
---

- (decision) AD-30: adopt hybrid prune — code default keep=5 below desired-state file below CLI flags; auto-prune obeys active/last-N/seed-pinned/undated floor while manual prune may override aggressively; prevents surprise deletion while preserving manual control
- (decision) C: lock the auto-prune protected floor to active + last-N + seed-pinned + undated, with future favorites joinable through desired-state data rather than a rule change
- (decision) B2: auto-prune uses reconcile --plan preview (show exact deletes) and reconcile executes/logs them; no interactive prompt, preserving future daemon use
- (assumption) File-lock primitive/filesystem behavior unresolved; record OS-level file lock as the requirement and select the portable implementation during Phase 4 architecture
- (decision) AD-31: serialize all history.jsonl writers with a file lock; prevents torn-line interleaving without changing append-only behavior
- (event) Phase 4 decision handoff seeded; full PRD remains to be drafted
- (decision) Correction: manual prune escape hatch is --keep 0/whole-cache plus --prune-pinned; planner deletes only candidates outside the protected floor
- (change) Merged the manual-prune bullet so --keep 0 and --prune-pinned read as one rule; validation formatting note closed
- (decision) Tightening: whole-cache deletion is NOT offered; --keep 0 plus --prune-pinned removes everything except active + undated entries
- (decision) Tightening: prune execution appends exactly one history line with trigger=prune including counts; dry-run appends nothing
- (decision) Tightening: history-lock failures surface as typed errors; AD-31 scoped to local POSIX, NFS/synced behavior explicitly out of scope until evidence appears
