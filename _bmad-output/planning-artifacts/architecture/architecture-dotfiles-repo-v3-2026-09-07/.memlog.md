---
scope: Phase 3 State Awareness spine extension
purpose: build-substrate
altitude: feature
updated: 2026-09-07T10:15
---

- (constraint) Inherit parent AD-1..AD-20 as binding: hexagon inward, JSON store FS-authority, state_root runtime-owned, symlink swap, env-overrides, SHA-256, staging-dir, derivation graph, seed+R2, sync imperative, nested hexagon, domain purity, cross-package boundary, hardlink, current-wiring, backend abstraction, separate binary
- (decision) AD-21 invalidation is hash-compare only: recompute spine input hashes, compare vs meta.json, regenerate minimal stale set plus cascade; no new hash formulas or cache layout
- (decision) AD-22 doctor owns three-way repair: current.json vs current-symlinks vs cache entries; quarantine rename-aside never rm-rf; repair appends history trigger=doctor and is idempotent
- (decision) AD-23 history torn-tail policy: readers skip plus warn, truncate only under doctor --repair
- (decision) AD-24 eviction keeps active plus last-N (default 5) plus seed-pinned; prune explicit with dry-run default-safe; removals logged
- (decision) AD-25 new port IInvalidationQuery in ports, DoctorUseCase plus PruneUseCase in application, CLI doctor plus cache prune --verify; layering test unchanged
- (event) spine finalized (fast path, inherits AD-1..AD-20, adds AD-21..AD-25)
