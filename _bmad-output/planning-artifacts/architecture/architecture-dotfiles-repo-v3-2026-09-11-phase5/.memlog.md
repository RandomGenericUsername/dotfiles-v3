---
scope: Phase 5 Reactive Runtime: continuous reconciliation via a user-scoped daemon, bounded filesystem watching of declared intent + spine inputs, and reactive execution through the existing use cases
purpose: build-substrate
altitude: feature
updated: 2026-09-11T20:00
---

- (event) Phase 5 architecture spine run started (coaching path, spine-only deliverable); input = epics-dotfiles-runtime-phase5.md scoping draft + docs/99 roadmap Phase 5
- (constraint) Inherit Phase 2 spine (architecture-dotfiles-repo-v3-2026-08-18) AD-1..AD-20 binding read-only, original ids never renumbered
- (constraint) Inherit Phase 3 spine (2026-09-07) AD-21..AD-25 binding read-only
- (constraint) Inherit Phase 3 delta (delta-phase3-digest-2026-09-10) AD-26/AD-27 binding read-only
- (constraint) Inherit Phase 4 spine (2026-09-11) AD-30/AD-31/AD-32 binding read-only
- (constraint) AD-12 synchronous imperative execution stays: commands remain source of truth and must work with the daemon absent; the daemon is additive
- (constraint) AD-21/AD-32 invalidation is independent hash-compare only; the daemon must never trust cached computed state or a same-invocation hash
- (constraint) AD-25/AD-1/13/14 layering: domain pure, adapters own I/O, application orchestrates; new units land in existing layers, no new store
- (constraint) AD-30 prune floor (active + last-N + seed-pinned + undated) is the only delete authority; a daemon is the highest-risk delete path
- (constraint) AD-31 one OS file lock serializes all history.jsonl writers; the daemon joins it
- (constraint) AD-11 seed mutex + reconcile/set state mutex serialize concurrent CLI + daemon; daemon must not race them
- (constraint) AD-4 history.jsonl is append-only must-not-lose; reactive events must not tear or drop lines
- (assumption) Next free AD id is 33 (highest existing is AD-32; AD-28/29 unused); draft proposes AD-33..AD-36
- (decision) AD-33 daemon lifecycle: the reactive runtime runs as a systemd --user service (Type=simple), supervised by systemd with Restart=on-failure + backoff; control surface is systemctl --user, not a self-managed pidfile. Binds the daemon process + its provisioning. Prevents silent death (unrecoverable watcher) and Hyprland-reload amnesia (autostart-spawned process dying with the compositor reload).
- (constraint) Owner decision: provisioning architecture owns the daemon's configuration (systemd user unit file, enablement). The runtime only provides the executable. Preserves the locked 'roles provision, runtime executes' split.
- (constraint) Consequence: the daemon CLI surface is a foreground (systemd ExecStart); NO daemon start/stop (systemctl is the control surface). Drops the draft's start/stop wording.
- (assumption) User value: desktop dotfiles daemon is recoverable-but-not-critical; heavy supervision/alerting is out of scope, restart-on-failure is sufficient.
- (question) Open (implementation, later epic/story): provisioning a user systemd unit is a NEW pattern (no existing role writes ~/.config/systemd/user/). Enabling the unit must work whether or not bootstrap runs inside a user D-Bus session (systemctl --user enable vs symlink into <target>.wants/). Also decide the unit's session target (graphical-session.target) + env import.
- (constraint) Correction to the corrupted 'Consequence' entry above (shell interpolated backticks): the daemon CLI surface is exactly one foreground command, dotfiles-runtime daemon run, which is the systemd ExecStart. There are NO daemon start/stop subcommands; systemctl --user is the control surface.
- (direction) User reframed the reactive runtime: event -> react with NO polling anywhere. Concrete envisioned use cases are shell-plane (AGS bar): (1) recording indicator that appears on recording start and disappears on stop, reacting to the capture lifecycle (currently the bar polls capture-tool status every 500ms, recording.tsx); (2) a wifi-icon action that starts a speed test and animates/changes the icon while it runs, reacting to that process. General requirement: events -> reaction.
- (constraint) No polling for STATE. State transitions must be event-sourced. Timers are allowed ONLY for continuous display (elapsed clock tick, animation frames), never to rediscover state.
- (constraint) Grounding: the bar already has the correct event pattern for network (network.tsx uses Astal createBinding on NetworkManager properties, i.e. GObject/D-Bus signals). recording.tsx is the anti-pattern (GLib.timeout_add polling capture-tool status). capture-tool is a short-lived Python CLI writing state.json; the recorder child is spawned by it and it exits, so no exit event reaches the bar.
- (question) RESCOPE: what does the Phase 5 spine govern? (a) the shell reactivity plane (AGS/GJS event model: Astal bindings, D-Bus subscriptions, child-process events) plus the runtime-to-shell notification boundary; (b) the Python runtime reconcile daemon as originally drafted; (c) both, as two layers joined by an explicit contract. The draft conflated these.
- (decision) AD-35 (paradigm): orchestrate the core, choreograph the edges. Keep the runtime synchronous hexagonal use cases and the wallpaper-set pipeline exactly as-is (preserves AD-12). Add an event contract only at SUBSYSTEM boundaries (bar <-> tools <-> runtime). Do NOT rewrite the internal pipeline or the batch tools as event mesh / daemons.
- (decision) Tool classification (binds future stories): batch commands that run-and-exit stay CLIs (CSG, WEG, ITR); processes with a lifetime are jobs that own their child and publish lifecycle+domain events (capture controller/recorder, speed test); interactive apps publish their own domain events at the meaningful moment (ICME on save, NOT on exit); existing D-Bus services are bound directly and NEVER wrapped (NetworkManager).
- (constraint) Two event kinds must not be conflated: lifecycle events (started/progress/finished) that a supervisor can infer from owning the child; and domain events (recording paused, mappings saved, test result) that only the tool itself can emit.
- (direction) The event contract is the single shared artifact between the Python runtime world and the GJS bar world; neither imports the other's code. Runtime side implements it via an internal port + D-Bus adapter (core stays transport-agnostic).
- (decision) AD-34 event contract and boundary: the wire contract is a SHARED language-neutral spine companion (event-contract.md, mirroring shared-data-contract.md), versioned in the interface name (org.dotfiles.Events1). The runtime implements its side via ports/event_bus.py (IEventPublisher/IEventSubscriber) plus adapters/dbus_event_bus.py; the bar is a thin consumer binding the same names. Binds both worlds. Prevents the bar and the runtime from depending on each other's code, and prevents the runtime core from depending on D-Bus.
- (constraint) Primary invariant: the bar and the runtime depend on the CONTRACT, never on each other's code; the runtime core depends on the PORT, never on D-Bus; the transport lives in an adapter.
- (question) Open: contract drift-enforcement method. Options: (1) hand-written per-side constants + a small drift test per language against the documented names; (2) a single machine-readable names file both sides read (no generator); (3) full codegen from D-Bus introspection XML. User asked what codegen means; recommendation pending.
- (decision) AD-36 loop safety: (A) the daemon reacts ONLY to a declared trigger set (spine derivation inputs + declared intent); trigger roots are file-wise disjoint from everything the runtime WRITES. Intent (desired.json) relocates OUT of state_root into a config location, so state_root stays pure runtime output. Plus (C) a last-converged input-hash backstop: on any event, recompute input hashes; if unchanged, no-op. Prevents feedback loops (watcher firing on the runtime's own writes) and makes the daemon idempotent.
- (decision) AD-12 delta (owner accepted): Phase 5 lifts AD-12's prohibition of daemon/event bus at the EDGES only. The core stays synchronous and imperative; commands must work with the daemon absent; the pipeline is unchanged. AD-12 is not deleted, its domain is narrowed to the core. Automatic mode is opt-in, observe-only first, and every automatic delete stays bounded by the AD-30 floor.
- (decision) AD-37 tool classification: batch tools that run-and-exit stay commands (CSG, WEG, ITR); processes with a lifetime are jobs that own their child and publish lifecycle+domain events (capture controller/recorder, speed test); interactive apps publish their own domain event at the meaningful moment (ICME on save, not exit); existing D-Bus services are bound directly and never wrapped (NetworkManager). Prevents every tool from independently deciding whether it is a daemon.
- (decision) AD-34 enforcement (owner accepted): rung 2, NO codegen. event-contract.json holds the names as the test-time source of truth; each side bakes constants into code and a small drift test per language asserts they match the JSON. Validated: no off-the-shelf D-Bus codegen exists for Python or JS/GJS (gdbus-codegen is C/DocBook only and absent; dasbus/dbus-fast ship no generator; dbus-fast has runtime introspection only). A custom generator was prototyped (108 lines) and is feasible but not adopted to avoid owning bespoke tooling plus build hooks.
- (assumption) D-Bus transport is the SESSION bus (per-user). The bar/GJS side uses Gio (built into gjs); the Python side needs a client library whose choice is deferred to implementation and must fit the synchronous core (favors GLib/sync over asyncio-first).
- (event) All Phase 5 architecture decisions locked; distilling spine + event-contract companion.
- (constraint) Correction to the codegen rationale (reality-check): the absolute claim 'no D-Bus codegen exists for Python' is false (python-sdbus ships a generator; dbus-python-client-gen exists; gdbus-codegen is C/DocBook-only; JS/GJS has no direct generator and uses runtime proxy wrappers). The DECISION stands for a different reason: a generator would couple the contract to an unchosen client library, and for four signals hand-written constants plus a drift test is cheaper to own. No codegen.
- (decision) AD-38 one bus-name owner + trust boundary: exactly one process (the runtime daemon, dotfiles-runtime daemon run) owns the well-known name org.dotfiles.Events and is the signal hub. Jobs/tools emit through the hub (or do not publish when the daemon is absent) and never RequestName an org.dotfiles.* name. The contract is session-bus only; consumers validate sender/topic; no automatic deletion may be triggered by an untrusted publisher.
- (decision) AD-39 watched root set: an explicit ENUMERATED allowlist (the four derivation-input dirs + the relocated desired.json). It EXCLUDES the consumer-pointer destinations (config/ags/colors.css, config/gtk-*/colors.css, config/rofi/colors.rasi) and all of state_root. No parent-directory watches (no recursive watch of <install>/config). The last-converged hash backstop is in-memory (if ever persisted, it lives under state_root, never a trigger root). Pins the relocated desired.json path, retiring the deferred intent-location item.
- (decision) AD-40 watch strategy: watch the bounded enumerated roots only; coalesce/debounce bursts into one reconcile; on IN_Q_OVERFLOW do a full re-scan of the roots; NO polling fallback (owner rejects polling). Mechanism detail (inotify lib) deferred, but inotify is not recursive so the implementation watches each root dir explicitly.
- (decision) AD-41 observability and safety: automatic convergence ships OBSERVE-ONLY first; status via systemctl --user plus a read-only inspect surface; a kill-switch (stop the unit); every automatic action (regenerate/prune) logged with its trigger; delete-audit retained. Safety is not optional for the first automatic delete path.
- (decision) AD-42 reactive history trigger: a daemon-initiated converge appends history with a new trigger value (reactive). Adding it is a shared-data-contract change shipping IN THE SAME change as the inspect validator update. No unit may introduce a history trigger value absent from the contract.
- (decision) Amendments from the reviewer gate: AD-34 also pins DomainEvent payload SCHEMAS (event-contract.json carries names AND schemas; a shape change is a breaking change -> Events2). AD-37 ownership is transitive (if a wrapper spawns the observed process and exits, the observed process is the job); the capture indicator is driven by DOMAIN events only, never JobStarted/JobFinished. Convention: a tool state file is a write-behind projection, never the authority for a live consumer; continuous display (elapsed) derives from a monotonic clock + a recorded start, not a file re-read. Inherited AD-19 operational envelope binds (absence/recovery when systemd/bus/inotify missing).
- (decision) Confirmatory-gate tightenings: (D-2) a lifetime job is the long-lived D-Bus-capable owner that outlives and holds the observed child; a wrapper must never exit while a child it spawned still lives; if unavoidable it hands ownership to the daemon/hub. (NEW-1) the hub pins a caller-to-topic authorization mapping and rejects non-conforming Emit; producer is hub-validated, never self-asserted. (NEW-2) the hub exposes GetTopicState(topic) as the sole domain-state hydration path; consumers never read write-behind files. (D-5) event-contract.json carries allowed enum values, not just types. (D-6) file roots are watched via their immediate parent directory filtered to the exact filename (atomic-replace safe); no watches above a root's immediate parent.
- (event) spine finalized
- (decision) C3 trust posture (validate-gate ruling): the session bus is a TRUSTED SAME-USER domain; per-sender identity authorization is unenforceable and buys nothing (the attacker is the user; no privilege boundary). AD-38 is reframed: the hub is the SOLE owner of org.dotfiles.Events (no shadow hub) and performs STRUCTURAL validation (schema/size/depth/rate) on every Emit, not identity authorization. Damage is bounded by the AD-30 floor plus observe-only-first. Threat model documented. Option D (unix socket with SO_PEERCRED) is the future path only if multi-user or untrusted plugins arrive.
- (decision) C1 resolution (validate-gate): the daemon's trigger surface is the watched SPINE roots only (the four derivation inputs + the relocated intent doc). No daemon topic triggers. ICME save is observed via the watched spine icon-mappings file; icme.saved is a BAR/UI domain event, not a daemon trigger. The dev-in-repo edit path is handled by bootstrap propagation, not the daemon. AD-39 is corrected: watched roots are the PINNED spine locations, NOT whatever derive.find_* returns (which can be a repo path on dev). AD-36 stays file-only; no contradiction with AD-37/42.
- (decision) Caveat fix folded as AD-43 + a story: derivation inputs resolve from the SPINE only. (1) derive's implicit repo-ancestor fallback becomes an explicit opt-in dev override (env-gated, e.g. DOTFILES_DEV_INPUTS_ROOT); in production a missing input is a loud typed failure, never a silent repo read. (2) provisioning verify asserts input CONTENT not just containers: icon-mappings/icons.yaml + siblings, non-empty icon-templates and config/color-scheme-generator/templates, plus config/weg/effects.yaml. (3) runtime doctor/check-inputs provenance tripwire detects a repo-sourced input and surfaces it. Pre-existing debt (rt-1-11) surfaced by Phase 5.
- (decision) C2 resolution (validate-gate): the hub is broker AND job registry. It exposes BeginJob(kind)->job_id, ReportProgress(job_id,fraction), EndJob(job_id,exit_code); the HUB emits JobStarted/JobProgress/JobFinished (sole emitter, AD-38). The hub allocates job_id and carries an epoch; on restart the epoch bumps and it emits JobsCleared (or synthetic JobFinished per outstanding job) so consumers never hold stale state. AD-37 wording becomes: a lifetime job reports via hub methods; the hub emits the lifecycle signals.
- (decision) C4 resolution (validated by 4 agents + live test): AD-33 supervision = Type=dbus + BusName=org.dotfiles.Events (VERSIONLESS; interface stays org.dotfiles.Events1) + Restart=always + RestartSec backoff + tuned StartLimitIntervalSec/Burst + Requires=/After=dbus.socket + WantedBy=/After=graphical-session.target. The daemon does RequestName(DO_NOT_QUEUE), fails fast non-zero on contention, NEVER exits 0 before owning the name (false-success hole), and releases the name on SIGTERM. Name loss is a CLEAN stop, so Restart=always is mandatory. Provisioning enables via graphical-session.target.wants/ when no live user manager. Daemon absent/stopped = reduced functionality (accepted). Residual: a wedged-but-name-owning daemon is undetected; WatchdogSec/sd_notify deferred.
- (decision) C5 resolution (validate-gate): the daemon converges on start, gated by a persisted last-converged input-hash record stored under state_root (an output location, never a watched root). On start it loads the record; if inputs differ or no record exists it converges, then persists on success. This upgrades AD-36's backstop from in-memory to durable across restarts, so downtime input changes are reconciled. Unseeded (no current.json) is a benign no-op: log and wait, never an error, never a restart; the daemon never seeds (AD-11). Observe-only first means a start-time converge logs intent.
- (question) C6 finding (validate-gate), pending ruling: the history trigger enum is inconsistent AND AD-30's prune audit line is UNIMPLEMENTED. Evidence: validator _VALID_HISTORY_TRIGGERS = seed|set|reconcile|force|regenerate|doctor (6); shared-data-contract.md still says seed|set|reconcile|force (4); code writes only seed/set/reconcile/regenerate/doctor (force unused); append_history is called ONLY from reconcile.py and seed_cache.py, so a prune execution writes NO history line, contradicting AD-30 trigger=prune. AD-42's premise 'no trigger absent from the contract' is therefore already false.
- (decision) AD-44 machine-enforced shared contracts: every cross-boundary shared contract (history trigger enum, current.json, meta.json, event contract) has EXACTLY ONE machine-checkable definition (code constants / JSON schema). Prose documents are descriptive, never authoritative; a drift test fails when the doc and the definition disagree. Fixes the proven brittleness (shared-data-contract said 4 trigger values, validator enforced 6).
- (decision) C6 fold-in: the trigger enum is single-sourced; values become seed|set|reconcile|regenerate|doctor|prune|reactive. AD-30's prune audit line is IMPLEMENTED as its own remediation story (Phase 4 defect) before the daemon. Dead value 'force' dropped. reactive added with the validator in the same change (AD-42 intent).
- (decision) Remaining validate-gate folds (H-tier): the daemon JOINS the existing .seed.lock and .history.lock mutexes per action, never holding across sleeps. Contract semantics pinned: signals are at-most-once; consumers MUST hydrate-then-track via subscribe-before-read + a per-topic monotonic seq; payloads validated at runtime for schema/size/depth with caps; additive optional fields are non-breaking, a removed/retyped/required field is Events2. AD-19 correction: absence/recovery is a Phase-5 convention, NOT an inherited AD-19 property. Assumption recorded: single active graphical session per user (state_root is per-user); multi-session scoping is deferred.
- (decision) Update pass applied: AD-33..AD-44 rewritten to the validated decisions (C1-C6, AD-43 caveat, AD-44 machine-enforced contracts); event-contract.md/json updated (hub methods/registry, epoch-scoped seq, JobsCleared, subscribe-before-read, structural caps, additive-vs-breaking + Events1/2 coexistence, capture elapsed anchor); epics reconciled with prerequisite remediation R-1..R-4. Confirmatory adversarial pass: all C1-C6/AD-43/AD-44 CLOSED; two refinements applied (epoch-scoped seq comparison by pair; quantified recursion + atomic-replace watch persistence). lint_spine 0 findings; contract JSON valid.
- (event) spine finalized after Update pass
