---
topic: Full-scope PipeWire audio control: AGS bar indicators + follower-anchored audio popup (outputs, per-app streams w/ routing, inputs, recorders)
updated: 2026-09-21T11:07
---

- (decision) Source of truth: docs/linux-wayland-pipewire-ags-audio-control.md — AGS is a frontend to PipeWire/WirePlumber, never a second audio system; dynamic discovery, no wpctl polling.
- (decision) Data architecture: AstalWp-0.1 collections (speakers, microphones, streams=playback apps, recorders=recording apps, devices) with *-added/*-removed signals = event-driven reactive UI. Stream ids are identity; two streams from one app are two rows.
- (decision) Bar output indicator: LEFT-click opens the audio popup; RIGHT-click launches pavucontrol. Scroll adjusts volume.
- (decision) Full scope, nothing deferred: per-application routing (Stream.target-endpoint -> speakers) is in v1.
- (decision) Mic lives on the bar too. Existing pipeline has capture-tool/default/mic.svg (capture-tool-mic.svg); may deserve its own microphone group for bar contrast-guard membership.
- (decision) Audio popup anchors UNDER the triggering bar icon (follower anchor), never a fixed screen edge. The settings panel's fixed top-right anchor is an acknowledged error being fixed in a separate branch.
- (decision) Visual language inherited, not invented: GUI-unification spec tokens (glass alpha(@color_00,.94) + 1.5px @color_04 border + radius 12; cards alpha(@color_01,.18)+hairline; selection @color_10 fill w/ FOREGROUND text; action @color_06; slider track alpha(@color_foreground,.16), fill @color_13, thumb @color_15).
- (decision) Reuse AGS settings-panel primitives (PanelCard, LevelSlider, catcher window + Esc/click-outside dismissal) rather than new visual patterns.
- (event) Bound new UX run folder ux-dotfiles-repo-v3-audio-2026-09-18; ux-dotfiles-repo-v3-2026-09-18 is claimed by the settings-panel run.
- (event) Isolated work: git worktree dotfiles-repo-v3-audio, branch feat/pipewire-audio-ags, branched from master HEAD eb0dede (master has in-progress settings-panel changes).
- (assumption) Stakes: personal/internal desktop dotfiles; single user (juan david); no regulated content.
- (event) Rendered key-screen mocks: .working/key-bar-indicators.html (bar cluster + glyph states + contrast proof) and .working/key-audio-popup.html (3 states: everything playing, muted+empty, routing open); opened in Google Chrome.
- (assumption) Mock content: Output device list inline in card; Recording as its own card with live dot; Applications card collapses when empty.
- (decision) No glyphs anywhere: every system icon is an ITR-rendered SVG resolved through IconRegistry. Bar output uses the existing volume group; mic uses a NEW microphone group (mic-on, mic-off).
- (decision) Mic single-source: microphone/default/mic-on.svg is canonical; capture-tool's mic variant re-exports it (capture-tool-mic.svg) so the capture UI keeps its frozen variant name with no duplicate art.
- (decision) Mic live state: the mic-on icon shows in the bar with the green live dot when any wp.recorders stream is active; dot is the #2ecc71 literal signal green (same semantic exception as the LIVE badge / recording dot).
- (change) Row layout standardized: top line = leading glyph + name/sub + trailing routing; slider line = track + percentage. The percentage never sits on the title line.
- (event) Mocks re-rendered with real repo SVGs (.working/assets/ via itr render): volume-* levels, microphone-mic-on/off, plus the real bar icons. Reopened in Chrome.
- (event) Rendered key-alternatives.html (A/B for the 3 open decisions) and opened in Chrome.
- (decision) Decision 1 = B: output devices use the settings-panel subview pattern (current output card + 'Change >' swaps the whole panel to a device list with a back arrow). Not inline.
- (decision) Decision 2 = A: recording streams get their own 'Recording' card, separate from Applications (clear playing-vs-recording split).
- (decision) Decision 3 = A: empty sections collapse/hide entirely (no 'No apps playing' placeholder); popup height follows the live graph.
- (event) All three open decisions resolved by juan david; proceeding to Finalize spines then implementation.
- (event) Spines finalized (status: final): DESIGN.md + EXPERIENCE.md; mockups promoted to mockups/ (3 HTML + rendered assets).
- (event) IMPLEMENTATION BLOCKER discovered: installed AstalWp (libastal-wireplumber-git r973) typelib does NOT expose streams/speakers/microphones/recorders collections (undefined at runtime; GIR on disk is stale/mismatched). Real surface: wp.nodes (media-class enum: 1=Audio_Sink, 2=Audio_Source), wp.devices, default-speaker, default-microphone. Per-application stream collections are unavailable, so per-app volume/routing + recorders sections cannot be built on this binding as documented.
- (decision) RESOLVED API model for installed AstalWp (libastal-wireplumber-git r973): use wp.nodes filtered by media-class enum (1=Audio_Source/mic, 2=Audio_Sink/output, 3=Stream_Input_Audio/recorder, 4=Stream_Output_Audio/playback app) + wp.devices + default-speaker/default-microphone. The speakers/streams/recorders getters are NOT compiled in this build (undefined at runtime) despite the GIR listing them; get_nodes()/get_devices() work. Verified live: a pw-play stream appears as a class=4 node and disappears when it ends.
- (event) HOLD: main repo advanced to commit 27ce400 (settings-panel bluetooth discovery, per-connect PipeWire audio routing, follower anchoring, piwewire-audio pkg). My worktree is based on eb0dede and overlaps app.tsx/state.ts/style.css/packages.yaml. Plan: wait for main to finish, then rebase feat/pipewire-audio-ags onto new master and re-apply my edits against the new baseline. Cause of the 'setIconCenterX missing export' bar crash: two provisioning runs (main + worktree) writing the same host spine (~/.local/share/dotfiles), interleaving settings.tsx/state.ts.
- (event) Post-rebase bar outage: a dropped clampVolume import in bar/widgets/audio.tsx threw ReferenceError at render and killed the whole Bar window (no layer surface). ags bundle cannot catch runtime-only references. Fixed, committed, redeployed single file, bar + notifications restarted; live screenshot confirms volume% + mic render with live values.
