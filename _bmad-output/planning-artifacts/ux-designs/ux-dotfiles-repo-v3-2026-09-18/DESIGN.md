---
name: AGS Settings Panel
description: macOS-style desktop control-center panel rendered by AGS (GTK4) for the Hyprland dotfiles desktop. Visual identity is a project of the existing GUI-unification language, driven by the runtime wallpaper palette.
status: draft
updated: 2026-09-18
colors:
  # Frontmatter tokens pending Discovery. Values will reference the runtime
  # palette tokens (color00..15 / background / foreground) by name, per the
  # GUI-unification UX spec, rather than restating generated hex.
typography: {}
rounded: {}
spacing: {}
components: {}
---

<!-- Body pending Discovery + Finalize. -->
