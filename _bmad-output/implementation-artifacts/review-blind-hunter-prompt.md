# Blind Hunter — Adversarial Review

Invoke the `bmad-review-adversarial-general` skill on this diff.

## Context

Story 2.2: Container Processing via Pre-Resolved Host Config
- Adds serializers for AppSettings (TOML) and EffectsCatalog (YAML)
- Adds ContainerProcessor implementing EffectProcessorPort
- Wires container dispatch in factory and CLI

## Diff

The diff is at `_bmad-output/implementation-artifacts/diff-story-2.2.txt`
