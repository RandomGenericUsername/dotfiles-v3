---
baseline_commit: 054504e
---

# Story 1.7: csg adapter with env override

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a developer,
I want a csg adapter that directs output via env override,
So that palettes land in the cache without editing provisioning's settings.toml.

## Acceptance Criteria

1. **Given** a valid wallpaper image file `wallpaper_path: Path` that exists and is readable (validated via `hash_file(wallpaper_path)` producing 64-char `wallpaper_hash`), and a target cache palette entry `state_root/cache/palettes/<ph>/` where `ph = palette_entry_hash(wallpaper_hash, template_set_hash)` computed from `adapters/hashing.py` (with `template_set_hash = canonical_hash_dir(<install>/config/color-scheme-generator/templates/)` READ-ONLY spine input), and `output_dir: Path = state_root/cache/palettes/<ph>` (not yet existing), **When** `CsgAdapter.generate(wallpaper_path: Path, output_dir: Path) -> PaletteEntry` (implementing `IColorSchemeGenerator`) is invoked, **Then** it invokes the `csg` binary via `subprocess.run` with env `COLORSCHEME__OUTPUT__DIRECTORY=str(output_dir)` (literal double-underscore key per `shared-data-contract.md` Env-override protocol) **and** `COLORSCHEME__OUTPUT__OVERWRITE="true"` (required to allow overwrite inside staging), **and** passes `wallpaper_path` as positional arg with `--format yaml --format conf --format gtk.css` (three artifacts that define `PaletteEntry.artifact_hashes`), **And** `csg` writes exactly `colors.yaml`, `colors.conf`, `colors.gtk.css` into `output_dir` (verified via `output_dir.is_dir()` and files exist), **And** the method returns a `PaletteEntry` with `hash_algorithm=="sha256"`, `kind=="palette"`, `entry_hash==ph`, `source_wallpaper_hash==wallpaper_hash`, `input_template_hash==template_set_hash`, `artifact_hashes=={colors.yaml: hash_file(...), colors.conf: hash_file(...), colors.gtk.css: hash_file(...)}` computed via `adapters/hashing.hash_file` binary chunked, `generated_at==ISO-8601 UTC` (`datetime.now(UTC).isoformat().replace("+00:00","Z")`), **And** the provisioning-rendered `settings.toml` (read-only via discovery, never written) remains untouched — verified by stat mtime unchanged before/after (AC 1, AD-7, AD-2, shared-data-contract palette meta.json).

2. **Given** a `CsgAdapter` instance configured for local or container runtime mode (resolved externally; adapter itself takes no `runtime.mode` flag — container mode forwarding is an env passthrough property tested in integration), **When** the adapter runs in container mode (`csg` itself configured to use `podman`/`docker` via `oci-runtime` — provisioning owns engine install), **Then** the `COLORSCHEME__OUTPUT__DIRECTORY` override is passed **INTO** the container environment (not just host process) — proven by the integration double-run test that uses zero `-o` flag and relies solely on the env override, where the host `tmp/out` still receives artifacts even when `runtime.mode==container` (the container's `RunConfig` must include `env={"COLORSCHEME__OUTPUT__DIRECTORY": host_target}` per `ARCHITECTURE-SPINE.md` AD-7 last sentence + `shared-data-contract.md` Env-override protocol note). The adapter MUST NOT perform any `settings.toml` rewrite or file copy to simulate this — only env injection (AC 2, AD-7 container-mode override).

3. **Given** `csg` binary availability, **When** `CsgAdapter.is_available() -> bool` (or equivalent health check, if exposed) is queried — OR `generate` is called when `csg` not on PATH / container image missing, **Then** `generate` raises a typed error that preserves subprocess stderr (for later error mapping in Epic 3/4) — either `FileNotFoundError("csg not on PATH")` or `RuntimeError(f"csg generate failed: {stderr}")` with `stderr` truncated to 2 KiB, non-zero exit code handled, timeout handled (see AC 4) — **and** the caller (future `ApplyWallpaperUseCase`/`populate_via_staging`) sees a clean exception, not an unhandled `CalledProcessError` or silent swallow. The adapter MUST NOT suggest install via custom message beyond preserving stderr — suggestion logic lives in CLI layer (Story 2.9 future), but the error type/message must be predictable for `OutputPort.error()` to serialize (AC 3, NFR-2 reliability, shared-data-contract).

4. **Given** subprocess-based invocation, **When** `csg generate` hangs or exceeds a configurable timeout (default 60s, parameter `timeout: int = 60` on the adapter constructor, overrideable per-call), **Then** the adapter kills the process via `subprocess.run(..., timeout=timeout)` which raises `subprocess.TimeoutExpired`, which is caught and re-raised as `RuntimeError(f"csg generate timed out after {timeout}s")` (or `TimeoutError`) with the partial stdout/stderr preserved; **And** `output_dir` is left without partial files (caller is responsible for staging-dir cleanup via `populate_via_staging` — adapter itself must not leave half-written `colors.yaml` if it created `output_dir` directly; recommended pattern: caller creates `output_dir` as the staging dir and passes it, so adapter writes straight into staging — adapter must not `shutil.rmtree` itself, just raise) (AC 4, Story 2.6 timeout pattern reused, cache-model.md staging-dir AC 3).

5. **Given** hexagonal boundary (AD-1, AD-14, AD-15), **When** the csg adapter is added, **Then** `domain/` stays pure (allowlist `dataclasses, enum, typing, collections, collections.abc, functools, re, __future__` only; no `os`/`subprocess`/`shutil`/`pathlib`), `ports/IColorSchemeGenerator` remains ABC with `generate` abstract method unchanged (no concrete class in `ports/`), adapter lives in `adapters/` only (allowed `os`/`subprocess`/`pathlib`/`hashlib` plus `runtime.adapters.hashing` + `runtime.domain.models`), cross-package forbidden set untouched (`provisioning, core, infrastructure, color_scheme_generator, wallpaper_effects_generator, icon_templates_renderer, config_assembler_engine, oci_runtime` — runtime may `shutil.which("csg")` but never `import color_scheme_generator`), and `uv run --directory src/runtime pytest` + `ruff check` + `ruff format --check` + `mypy --strict` + `tests/architecture/test_layering.py` all pass (37 layering checks). The adapter MUST use `adapters/hashing.py:hash_file` + `canonical_hash_dir` + `palette_entry_hash` for every hash — no `hashlib.sha256(data).hexdigest()` inline reimplementation (AC 5, AD-1, AD-14, NFR-1, NFR-10).

## Tasks / Subtasks

- [x] Task 1 — Create `adapters/csg_adapter.py` (or `adapters/csg.py` — pick one, document choice, use everywhere) implementing `IColorSchemeGenerator` (AC: 1, 5)
  - [x] **Location MUST be `src/runtime/src/runtime/adapters/csg_adapter.py`** (alternative `csg.py` is allowed but pick exactly one; this story uses `csg_adapter.py` for clarity — `csg.py` collides with external package name). **MUST be in `adapters/`**, NOT `domain/`/`ports`/`application` (domain allowlist forbids `os`/`subprocess`/`pathlib`; ports are ABCs only). Keep `domain/models.py` unchanged. No new domain entities — `PaletteEntry` + `PaletteArtifacts` already exist from Story 1.2.
  - [x] Imports allowed ONLY in this file: `subprocess`, `os`, `shutil`, `hashlib` (only if delegating to `hashing.py` — prefer importing `hashing`), `pathlib.Path`, `typing.Final/Literal`, `datetime` (`datetime.now(UTC)`), `hashlib` indirect via `adapters/hashing`. Do NOT add `os`/`subprocess` to `domain/` or `ports/`.
  - [x] Export typed class — strict `mypy` signatures:
    ```python
    from pathlib import Path
    from runtime.ports.color_scheme_generator import IColorSchemeGenerator
    from runtime.domain.models import PaletteEntry
    from runtime.adapters.hashing import hash_file, canonical_hash_dir, palette_entry_hash, HASH_ALGORITHM

    class CsgAdapter(IColorSchemeGenerator):
        def __init__(self, timeout: int = 60, csg_bin: str | None = None) -> None: ...
        def generate(self, wallpaper_path: Path, output_dir: Path) -> PaletteEntry: ...
            # wallpaper_path: absolute or relative Path to image file (exists, is_file)
            # output_dir: target palette entry dir (state_root/cache/palettes/<ph>), must not exist or must be empty staging dir
            # returns PaletteEntry with artifact_hashes + generated_at; raises FileNotFoundError / RuntimeError / TimeoutError
        def is_available(self) -> bool: ...  # optional health check: shutil.which("csg") is not None
    ```
  - [x] Choose filename once and use everywhere (imports, tests, layering). Recommended: `adapters/csg_adapter.py` (explicit, avoids shadowing external `color_scheme_generator` package). If `csg.py` preferred, rename consistently and update checklist.
  - [x] Keep `HASH_ALGORITHM` import from `adapters/hashing.py` (do not duplicate): `from runtime.adapters.hashing import HASH_ALGORITHM` — assert in tests that returned `PaletteEntry.hash_algorithm == HASH_ALGORITHM == "sha256"` literal.

- [x] Task 2 — Implement `generate` with literal env override, no settings.toml edit (AC: 1, 2)
  - [x] Resolve `wallpaper_hash = hash_file(wallpaper_path)` (chunked 64 KiB, binary, raises `FileNotFoundError`/`IsADirectoryError` if missing) — use this hash as `source_wallpaper_hash`.
  - [x] Resolve `template_set_hash`: locate spine templates dir READ-ONLY — prefer explicit injected path if `CsgAdapter` takes `templates_dir: Path | None` (recommended: `__init__(templates_dir: Path | None = None)` where `None` resolves via repo search `src/cli-tools/color-scheme-generator/src/color_scheme_generator/defaults/templates` or via `importlib.resources` if packaged; same resolution as `tests/integration/test_csg_determinism._find_csg_templates_dir`). If resolution fails, use sentinel `hash_file_bytes(b"no-dir")`? But for AC 1, expect dir exists; raise `FileNotFoundError(f"CSG templates dir not found: {templates_dir}")` if caller must supply it. Simpler: require caller to pass `output_dir` already computed as `cache_entry_path(state_root, "palettes", ph)` where `ph` precomputed by use case; then adapter only hashes `wallpaper_path` + reads templates dir to compute `ph` for return? Clarify responsibility: Option A (preferred per cache-model.md + Story 1.7 AC): adapter computes `ph` internally from `wallpaper_hash` + `canonical_hash_dir(templates_dir)` then validates that `output_dir.name == ph` (raises `ValueError` if mismatch — cache-key correctness). This keeps adapter authoritative for entry hash. Option B: caller computes `ph` and adapter just writes; but then adapter cannot fill `PaletteEntry.entry_hash`. Choose Option A and document.
  - [x] Compute `ph = palette_entry_hash(wallpaper_hash, template_set_hash)` (from `adapters/hashing.py`, validates 64-hex inputs). Assert `output_dir.name.lower() == ph` else `ValueError(f"output_dir hash mismatch: expected {ph}, got {output_dir.name}")`.
  - [x] Ensure `output_dir` parent exists (`output_dir.mkdir(parents=True, exist_ok=True)`); `output_dir` itself may already exist if caller used `populate_via_staging` staging dir (then it's the staging Path) — do not require empty check beyond what staging guarantees.
  - [x] Build `env = dict(os.environ); env["COLORSCHEME__OUTPUT__DIRECTORY"] = str(output_dir); env["COLORSCHEME__OUTPUT__OVERWRITE"] = "true"` — literal keys, double underscores, uppercase, per `shared-data-contract.md` Env-override protocol. Do NOT use `COLORSCHEME_OUTPUT_DIRECTORY` (single underscores) or lowercase. The adapter MUST NOT touch `settings.toml` — verify by checking `settings.toml` path (e.g., `src/cli-tools/.../settings.toml` or `XDG` discovered) mtime unchanged in tests.
  - [x] Build args: `[csg_bin or "csg", "generate", str(wallpaper_path), "--format", "yaml", "--format", "conf", "--format", "gtk.css"]` — intentionally NO `-o`/`--output` flag; output dir comes solely from env override to exercise container forwarding contract (AD-7). Add `timeout` handling (Task 4) and `capture_output=True, text=True`.
  - [x] Run `result = subprocess.run(args, capture_output=True, text=True, env=env, timeout=self.timeout)` — on success (`returncode==0`) verify files exist: `output_dir / "colors.yaml"`, `colors.conf`, `colors.gtk.css` are files (not dirs). On failure, raise `RuntimeError(f"csg generate failed (exit {result.returncode}): {result.stderr[:2048]}")` preserving stderr truncated.
  - [x] Compute `artifact_hashes = { "colors.yaml": hash_file(output_dir / "colors.yaml"), "colors.conf": hash_file(...), "colors.gtk.css": hash_file(...) }` via binary `hash_file` (not `read_text`).
  - [x] Return `PaletteEntry(hash_algorithm=HASH_ALGORITHM, kind="palette", entry_hash=ph, source_wallpaper_hash=wallpaper_hash, input_template_hash=template_set_hash, artifact_hashes=artifact_hashes, generated_at=datetime.now(UTC).isoformat().replace("+00:00","Z"))`.

- [x] Task 3 — Handle container-mode env forwarding (AC: 2)
  - [x] The adapter itself does NOT need to detect `runtime.mode` — it simply sets the host env dict and passes to `subprocess.run`. The container forwarding guarantee (AD-7 last sentence: "`csg` runs in container mode (podman/docker via `oci-runtime`): the output-dir override must be passed **INTO** the container environment, not just the host process") is a property of how `csg` is invoked when `runtime.mode==container`: `csg` internally builds a `RunConfig` with `env={"COLORSCHEME__OUTPUT__DIRECTORY": ...}` via `oci-runtime`. The adapter's job is to set the host env correctly; the integration test (`tests/integration/test_csg_determinism._run_csg_generate`) proves correctness by running with zero `-o` flag and asserting host `tmp/out` still receives files even when `csg`'s own config says `runtime.mode==container`. Document this in adapter docstring: "Container forwarding is ensured by `csg`'s own container_processor forwarding env into `RunConfig`; this adapter only sets host env — verify via determinism test."
  - [x] Do NOT attempt to manually set Docker/Podman `-e` flags or mount volumes — that is `csg`'s responsibility via `oci-runtime ContainerRuntimePort`. The adapter must not `os.environ["COLORSCHEME__OUTPUT__DIRECTORY"]` globally (process-wide side effect) — only pass `env=` to `subprocess.run`.
  - [x] Document in code comment + story completion notes that future `CachePopulator` calling `populate_via_staging(target, lambda staging: CsgAdapter().generate(wallpaper_path, staging))` automatically satisfies container forwarding because `generate`'s env injection works identically per-call.

- [x] Task 4 — Implement timeout + error preservation (AC: 3, 4)
  - [x] Constructor `timeout: int = 60` (default 60s, matches Story 2.6 + pywal/wallust timeout). Allow call-site override via `generate(..., timeout: int | None = None)`? Simpler: instance timeout only; docstring notes "override by constructing `CsgAdapter(timeout=120)`".
  - [x] Wrap `subprocess.run(..., timeout=self.timeout)` in `try: ... except subprocess.TimeoutExpired as e: raise TimeoutError(f"csg generate timed out after {self.timeout}s: {e.stderr[:2048] if e.stderr else ''}") from e` — or `RuntimeError` if project prefers; be consistent with `tests/integration/test_csg_determinism` which expects `NondeterministicCSGError` on determinism failure, not timeout. Choose `RuntimeError` with "timed out" substring so callers can match. Document chosen exception type in `IColorSchemeGenerator.generate` docstring if needed (ABC docstring is informal; keep adapter's doc precise).
  - [x] On non-zero exit, raise `RuntimeError` with `f"csg generate failed (exit {code}): {stderr[:2048]} stdout:{stdout[:500]}"` — include both streams truncated, as `tests/integration` does for artifact debugging. Do NOT `raise CalledProcessError` directly — wrap to keep domain error surface uniform (`RuntimeError` or custom `CsgInvocationError` if defined). If defining custom `CsgInvocationError(RuntimeError)`, export it from `adapters/csg_adapter.py` and document.
  - [x] Validate `wallpaper_path` early: `if not wallpaper_path.exists(): raise FileNotFoundError(wallpaper_path)`; `if not wallpaper_path.is_file(): raise IsADirectoryError(wallpaper_path)`. These should be raised before subprocess spawn, matching `hash_file` error semantics.
  - [x] Ensure no partial `output_dir` cleanup inside adapter — caller (`populate_via_staging`) owns staging cleanup (deletes on exception). Adapter must not `shutil.rmtree(output_dir)` on failure; just raise and let caller discard staging.

- [x] Task 5 — Tests: unit + integration, strict green (AC: 1, 2, 3, 4, 5)
  - [x] Create `src/runtime/tests/unit/test_csg_adapter.py` — **MUST** be under `tests/unit/` (not `domain`/`ports`), fast path (`pytest -k "not integration"`; no `pytest.mark.integration`). Mark **NOT** integration unless invoking real `csg` binary; unit tests mock `subprocess.run`.
  - [x] Cover at minimum (table: test → setup → expectation):
    | Test | Inputs | Expectation |
    |---|---|---|
    | `test_csg_generate_env_override_writes_to_output_dir` | `tmp_path/wallpaper.png` (reuse `tests/fixtures/wallpaper.png` bytes `619cd350...`), `state_root=tmp_path/state`, mocked `subprocess.run` that writes `colors.yaml/conf/gtk.css` into `env["COLORSCHEME__OUTPUT__DIRECTORY"]` and returns 0, plus real `canonical_hash_dir` for templates (use temp `templates/` dir with one `colors.j2`) | `CsgAdapter.generate` returns `PaletteEntry` with `entry_hash==palette_entry_hash(wallpaper_hash, template_hash)`, `artifact_hashes` match `hash_file` of written files, `COLORSCHEME__OUTPUT__DIRECTORY` in `subprocess.run(env=)` equals `output_dir`, no `-o` flag in `args`, `COLORSCHEME__OUTPUT__OVERWRITE=="true"` |
    | `test_csg_adapter_never_touches_settings_toml` | `settings.toml` mock file with mtime recorded before | after `generate`, `settings.toml` `stat().st_mtime` unchanged, no file written at `settings.toml` path, adapter never opens it |
    | `test_csg_adapter_container_env_passthrough` | call `generate` with `wallpaper_path` and staging `output_dir`, mocked `subprocess.run` captures `env` | assert `env["COLORSCHEME__OUTPUT__DIRECTORY"] == str(output_dir)` even when global `os.environ` does not contain it before call (host-only env would fail container test) |
    | `test_csg_adapter_raises_on_non_zero_exit` | mocked `subprocess.run` returns `returncode=1, stderr="backend not available"` | `RuntimeError` raised with stderr substring, `output_dir` existence not required to be cleaned (caller will), exception type is `RuntimeError` (or `CsgInvocationError`) |
    | `test_csg_adapter_timeout_raises` | mocked `subprocess.run` raises `subprocess.TimeoutExpired(cmd, timeout=60)` | adapter raises `TimeoutError` or `RuntimeError` containing "timed out" |
    | `test_csg_adapter_raises_on_missing_wallpaper` | `wallpaper_path` not exists | `FileNotFoundError` raised before `subprocess.run` called (verify mock not called) |
    | `test_csg_adapter_entry_hash_mismatch` | `output_dir` name not equal to computed `ph` | `ValueError` raised ("hash mismatch") |
    | `test_csg_adapter_artifact_hashes_are_hex64` | successful generate with mocked subprocess writing files | `PaletteEntry.artifact_hashes` values are 64-char lowercase hex, `hash_algorithm=="sha256"` literal, `kind=="palette"` |
    | `test_csg_adapter_is_available` | `shutil.which` patched to `"/usr/bin/csg"` vs `None` | `is_available()` returns True/False without raising |
    | `test_csg_adapter_no_settings_toml_rewrite_even_on_container` | mocked subprocess that simulates container mode (env forwarding) | still no `settings.toml` modification, only env injection |
  - [x] For mocked `subprocess.run`, simulate real `csg` side effect: `def fake_run(args, capture_output, text, env, timeout): Path(env["COLORSCHEME__OUTPUT__DIRECTORY"]).mkdir(parents=True, exist_ok=True); (Path(env["COLORSCHEME__OUTPUT__DIRECTORY"])/"colors.yaml").write_text('{"background":"#000"}\n'); ...; return CompletedProcess(args, 0, "", "")`. Use this to test file existence and `hash_file` reading.
  - [x] Create `src/runtime/tests/integration/test_csg_adapter_integration.py` — **integration** (`pytest.mark.integration`) that calls real `csg` binary if on PATH — **SKIP** loudly if missing (mirror `test_csg_determinism.py` pattern: `shutil.which("csg") is None => pytest.skip("csg not on PATH")`). This test proves AC 1 + AC 2 container forwarding when engine present: uses `tmp_path/state/cache/palettes/<ph>/` as output_dir, calls real `CsgAdapter().generate(tmp_wallpaper, output_dir)`, asserts three files exist, their `hash_file` matches returned `PaletteEntry.artifact_hashes`, and `output_dir` is under `state_root/cache/palettes`. Do not commit `tests/integration/.csg_*` artifacts beyond determinism existing one; this integration test writes to `tmp_path` only.
  - [x] Use `tmp_path` for `state_root`; use deterministic bytes (reuse `tests/fixtures/wallpaper.png` 74 bytes `619cd350...`); no `random`/`os.urandom`. Verify no orphan staging after each test via `list((state_root / "cache").glob(".staging-*")) == []` — though this adapter doesn't use staging itself, the future use via `populate_via_staging` will; unit tests ensure adapter doesn't leak `cache/.staging-*`.
  - [x] Verify adapter does NOT create `cache/.staging-*` itself — it writes directly to the `output_dir` given (which will be a staging dir when called via `populate_via_staging`). Test that `generate` does not call `populate_via_staging` internally.

- [x] Task 6 — Ensure zero layering debt and full green (AC: 5)
  - [x] Keep adapter in `adapters/csg_adapter.py` only; verify `domain/models.py` still imports only allowlist (`dataclasses`,`enum`,`typing`,`collections`,`collections.abc`,`functools`,`re`,`__future__`); no new imports in `domain/`/`ports`. Verify `ports/color_scheme_generator.py` remains ABC only (`IColorSchemeGenerator` + `generate` abstract).
  - [x] Verify no cross-package imports: grep `from provisioning` / `import provisioning` / `from color_scheme_generator` (external package) / `import color_scheme_generator` — should be zero in `src/runtime/src/runtime/**` (adapter may `shutil.which("csg")` but never `import color_scheme_generator`). Check `cli-output` is only allowed external besides `typer` via `uv.sources`.
  - [x] Run and require green:
    ```bash
    uv run --directory src/runtime pytest -q                 # full suite (unit + integration skipped if csg missing)
    uv run --directory src/runtime pytest tests/unit/test_csg_adapter.py -v
    uv run --directory src/runtime pytest -k "not integration" -q  # fast path
    uv run --directory src/runtime ruff check src/runtime/adapters/csg_adapter.py src/runtime/tests/unit/test_csg_adapter.py
    uv run --directory src/runtime ruff format --check src/runtime/adapters/csg_adapter.py src/runtime/tests/unit/test_csg_adapter.py
    uv run --directory src/runtime mypy --strict src/runtime/adapters/csg_adapter.py
    uv run --directory src/runtime pytest tests/architecture/test_layering.py -v  # 37 checks must pass
    ```
    All existing tests (78 from Stories 1.1–1.6: scaffold + domain + ports + determinism + hashing + cache) must still pass; new csg unit tests add coverage without breaking layering. `tests/architecture/test_layering.py` domain allowlist + no Path FS calls + ports-as-ABCs + cross-package forbidden set must pass.
  - [x] Confirm `src/runtime/src/runtime/adapters/__init__.py` stays minimal (no star imports that create cycles, no re-export that violates layering). Document choice of `csg_adapter.py` vs `csg.py` in module docstring.
  - [x] Verify non-goals are NOT included: no `IEffectsGenerator`/`IIconRenderer` adapters (Stories 1.8–1.9, WEG/ITR), no `IStateRepository` JSON store or `current.json`/`history.jsonl` (Story 1.10), no `SeedCacheUseCase` / `ApplyWallpaperUseCase` / `ReconcileDesktopStateUseCase` (Stories 1.11–1.13), no CLI `wallpaper set` (Story 1.13), no cache eviction/prune, no `current/` symlink repoint (Epic 2 Story 2.1), no `DesktopState` persistence — those are later. Resist adding them.

## Dev Notes

### Scope boundary — this story is CSG ADAPTER ENV OVERRIDE ONLY

Story 1.7 **defines** the palette-generation adapter that directs `csg` output via `COLORSCHEME__OUTPUT__DIRECTORY` env override into `cache/palettes/<ph>/`. It does **NOT** implement:

- Hashing helpers (Story 1.5 — already done, reuse `adapters/hashing.py:hash_file`, `canonical_hash_dir`, `palette_entry_hash` + `HASH_ALGORITHM`)
- Staging-dir population + hardlink (Story 1.6 — already done, reuse `adapters/cache.py:populate_via_staging`, `hardlink_or_copy`, `cache_entry_path` — future use case will compose `populate_via_staging(target, lambda staging: adapter.generate(wallpaper_path, staging))` but this story's adapter does NOT call `populate_via_staging` itself)
- WEG/ITR adapters with env overrides (Stories 1.8–1.9: `WALLPAPER__OUTPUT__DIRECTORY`, `ICON_RENDERER__OUTPUT__OUTPUT_DIR` / `ICON_RENDERER__COLOR_SCHEME__PATH`)
- `IStateRepository` JSON store or `current.json`/`history.jsonl` persistence (Story 1.10)
- `SeedCacheUseCase` / `ApplyWallpaperUseCase` / `ReconcileDesktopStateUseCase` (Stories 1.11–1.13)
- Any CLI command (`wallpaper set`, `inspect`, `cache list`) — those arrive later
- Cache eviction/prune (deferred to Phase 3+ per AR-10)
- Consumer symlink repoint (`current/` symlinks) — Epic 2 Story 2.1
- Actual backend logic beyond env-wrapped `csg generate` — no template rendering duplication

Resist adding adapter logic for weg/itr — write only the csg adapter, env literal keys, timeout handling, `PaletteEntry` construction, and tests.

### Source tree components to touch

| Path | Action | Notes |
|------|--------|-------|
| `src/runtime/src/runtime/adapters/csg_adapter.py` | **CREATE** | `CsgAdapter(IColorSchemeGenerator)` with `generate(wallpaper_path: Path, output_dir: Path) -> PaletteEntry`; literal env `COLORSCHEME__OUTPUT__DIRECTORY` + `COLORSCHEME__OUTPUT__OVERWRITE`; `subprocess.run` with timeout 60s; computes `PaletteEntry` via `adapters/hashing.hash_file` + `canonical_hash_dir` + `palette_entry_hash`; no `settings.toml` edit; container forwarding via env passthrough (AD-7) |
| `src/runtime/src/runtime/adapters/hashing.py` | **USE** (already exists) | Import `HASH_ALGORITHM`, `hash_file`, `canonical_hash_dir`, `palette_entry_hash` — do NOT duplicate hex validation or SHA logic; reuse `_is_hex64` pattern for validation if needed |
| `src/runtime/src/runtime/adapters/cache.py` | **USE** (already exists) | Future composition will use `populate_via_staging` + `cache_entry_path` — adapter itself does NOT call these; document future `populate_via_staging(target, lambda s: adapter.generate(wallpaper_path, s))` pattern in docstring |
| `src/runtime/src/runtime/ports/color_scheme_generator.py` | **USE** (ABC, already exists) | `IColorSchemeGenerator.generate(wallpaper_hash: str, template_dir: str, output_dir: str) -> PaletteEntry` is currently defined with 3-str args; **ADAPT** to `Path`-based signature for this story: implement `generate(self, wallpaper_path: Path, output_dir: Path) -> PaletteEntry` — update port ABC signature if needed to accept `Path` args (preferred: keep port accepting `Path` for FS-bound adapter; if port stays `str`-based, adapter must accept `Path` and port will be updated in this story with architectural sign-off: `generate(self, wallpaper_path: Path, output_dir: Path) -> PaletteEntry`). Document the change in completion notes and verify `test_layering.py` still passes (ports are ABCs, allowed `pathlib` import in ports? Check layering test allowlist — ports may import `pathlib` but domain may not; verify). If not allowed, keep port as `str` and adapter does `Path(wallpaper_hash)` conversion; prefer Path for correctness. |
| `src/runtime/tests/unit/test_csg_adapter.py` | **CREATE** | Unit tests for env override, no settings.toml touch, container passthrough, timeout, error preservation, artifact hash validation, entry_hash mismatch; **MUST NOT** place under `domain`/`ports`; mock `subprocess.run`, verify `env` literal keys, no `-o` flag |
| `src/runtime/tests/integration/test_csg_adapter_integration.py` | **CREATE** (optional but recommended) | Real `csg` binary integration test (skipped if `shutil.which("csg") is None`); proves three files written to `output_dir`, `PaletteEntry.artifact_hashes` match `hash_file`, `settings.toml` untouched; writes only to `tmp_path` |
| `src/runtime/tests/fixtures/wallpaper.png` | **USE** (already exists) | Deterministic 4×4 PNG (`619cd350f9ad67e2faf42a0b738ffa16c54bb55e6a3fb8a09d0e4bca4cf57a2c`) reuse for `hash_file` + `wallpaper_hash` |
| `src/runtime/src/runtime/domain/models.py` | **LEAVE ALONE** | No `hashlib`/`os`/`pathlib` imports; domain stays pure (AD-14). `PaletteEntry` + `PaletteArtifacts` + `WallpaperEntry` used via construction only |
| `src/runtime/src/runtime/ports/**` | **LEAVE ALONE** except possible `IColorSchemeGenerator` signature Path adaptation | ABCs only; no concrete classes |
| `src/runtime/src/runtime/adapters/__init__.py` | **LEAVE ALONE** or minimal | Stays lean; no cross-package star imports |
| `src/runtime/src/runtime/application/**` | **LEAVE ALONE** | Use cases arrive in Stories 1.11–1.13 |
| `src/runtime/src/runtime/cli/**` | **LEAVE ALONE** | CLI `wallpaper set` arrives Story 1.13 |

### Testing standards summary

- **Runner:** `uv run --directory src/runtime pytest` (`pyproject.toml: testpaths=["tests"]`, `pythonpath=["src","."]`, `requires-python>=3.14`). Unit csg tests run in fast path (`pytest -k "not integration"`); integration marked `pytest.mark.integration` skipped if `csg` missing.
- **Lint/type:** `uv run --directory src/runtime ruff check` (py314, line 100, double quotes); `mypy --strict` (`python_version=3.14`, allowlist-only for domain/ports). No new ignores; `adapters/csg_adapter.py` must be `ruff format` clean.
- **Fixtures:** Reuse `tests/fixtures/wallpaper.png` (74 bytes); `tmp_path` files via `write_bytes(b"...")` — no `random`/`PIL`. Artifact hash tests must verify `hash_file` binary chunked, not `read_text`.
- **Env override assertion:** Verify `subprocess.run` called with `env` containing `COLORSCHEME__OUTPUT__DIRECTORY == str(output_dir)` and `COLORSCHEME__OUTPUT__OVERWRITE == "true"` — literal keys, never single underscores or lowercase. Verify `args` contains no `-o`/`--output` flag (only `--format yaml/conf/gtk.css`).
- **Container forwarding:** Mocked test proves host `env` passthrough; integration test (if `csg` on PATH) proves `tmp/out` receives files even when `csg` config says `runtime.mode==container` (container Processor forwards env via `RunConfig`). Do NOT attempt to set Docker `-e` manually.
- **Timeout:** Monkeypatch `subprocess.run` to raise `TimeoutExpired`; assert adapter raises `TimeoutError`/`RuntimeError` with "timed out" substring.
- **Error preservation:** Non-zero exit test asserts exception message contains `stderr` substring truncated to 2 KiB.
- **No settings.toml edit:** Record `settings.toml` `stat().st_mtime_ns` before/after `generate` — must be identical.
- **Atomicity note:** Adapter writes directly into `output_dir` (which will be staging when composed with `populate_via_staging`); adapter itself does not create `.staging-*` — staging is caller's responsibility. Verify no `cache/.staging-*` created by adapter alone.

### Architecture extraction — what this story must respect

| Decision | Relevance | What to do |
|----------|-----------|------------|
| **AD-1 Hexagonal** | Subprocess I/O in adapters | Keep csg subprocess + env injection in `adapters/csg_adapter.py`; **MUST NOT** import `os`/`subprocess`/`pathlib` into `domain` |
| **AD-2 Layered cache key** | Key = hash of ALL inputs via `adapters/hashing.py` | Use `hash_file(wallpaper_path)` + `canonical_hash_dir(templates_dir)` → `palette_entry_hash` to compute `<ph>`; entry dir = `cache/palettes/<ph>` via `cache_entry_path` validation; `meta.json` later (Story 1.10) will record `input_template_hash`, but adapter must compute it now for `PaletteEntry` |
| **AD-3 JSON store / FS authority** | `meta.json` co-located, FS authority | Adapter writes palette artifacts into `output_dir` which will become `cache/palettes/<ph>/` with `meta.json` when wrapped by `populate_via_staging`; filesystem `cache/` is truth |
| **AD-5 State root** | Runtime owns `$XDG_STATE_HOME/dotfiles/` | Adapter takes `state_root: Path` or `output_dir: Path` injected by caller; never discovers XDG itself, never writes under install spine |
| **AD-6 Swap = symlinks** | No copying during swap | Cache dirs are write-once; adapter ensures write-once is preserved via staging composition (caller), not via copy |
| **AD-7 CSG/WEG env override** | Literal keys, no settings.toml edit, container forwarding | Use literal `COLORSCHEME__OUTPUT__DIRECTORY` + `COLORSCHEME__OUTPUT__OVERWRITE=true`; **MUST NOT** edit `settings.toml`; container mode passes override INTO container env via `subprocess.run(env=)` — proven by determinism test zero-flag pattern |
| **AD-8 SHA-256 versioned** | `meta.json: hash_algorithm: sha256` | `PaletteEntry.hash_algorithm == HASH_ALGORITHM == "sha256"` literal; `artifact_hashes` values are 64-char hex via `hash_file` |
| **AD-9 Staging-dir** | Population via `cache/.staging-<pid>/` + `os.rename` | Adapter writes into the `output_dir` given (which is staging when composed); do NOT call `populate_via_staging` inside adapter — document composition; caller handles TOCTOU race + cleanup |
| **AD-14 Domain purity** | Allowlist enforced | Domain imports only `dataclasses`,`enum`,`typing`,`collections`,`collections.abc`,`functools`,`re`,`__future__`; banned `os`/`subprocess`/`shutil`/`pathlib`/`io` — adapter is the only place with those |
| **AD-15 Cross-package boundary** | Runtime imports only `cli-output` | Do NOT `import provisioning` or `color_scheme_generator` package; adapter uses `shutil.which("csg")` + `subprocess.run(["csg", ...])` as black box (like `tests/integration/test_csg_determinism._run_csg_generate`) |
| **AD-11 First-run seeding** | Reads spine READ-ONLY | Adapter reads `templates_dir` READ-ONLY via `canonical_hash_dir` to compute hash; never mutates spine |
| **AD-12 Synchronous** | No daemon/async | Synchronous `subprocess.run` suffices; no async csg |
| **AD-19 Operational envelope** | CLI via `cli-output` | No `cli-output` usage inside adapter — adapter is pure I/O wrapper; error serialization via `OutputPort.error()` is CLI's job later |

### Previous story intelligence (Stories 1.1–1.6) — what exists now

- **Story 1.1 (scaffold):** package at `src/runtime/src/runtime/{domain,ports,adapters,application,cli}` + `tests/architecture/test_layering.py` (37 checks: domain allowlist, banned `subprocess`/`os`/`shutil`/`pathlib`/`io` in domain, no Path FS calls, ports are ABCs, cross-package forbidden set) + `pyproject.toml` (hatchling, `requires-python>=3.14`, `typer`, `cli-output` via `uv.sources`, `pytest` `testpaths=["tests"]`, `pythonpath=["src","."]`). All scaffold tests pass; layering test is the boundary enforcer. **Do not break it.**
- **Story 1.2 (domain):** `src/runtime/src/runtime/domain/models.py` defines 8 frozen dataclasses/enums: `WallpaperEntry(hash_algorithm:Literal["sha256"], kind:Literal["wallpaper"], content_hash, source_path, imported_at)`, `PaletteEntry(entry_hash, source_wallpaper_hash, input_template_hash, artifact_hashes:PaletteArtifacts, generated_at)`, `EffectsEntry`, `IconsEntry`, `MonitorWallpaperConfig(backend:BackendType, source_hash, fit_mode:FitMode, mpv_options, ipc_socket)`, `DesktopState(schema_version:Literal[2], ...)`. All `@dataclass(frozen=True, slots=True)`. Domain imports only allowlist. **Reuse `PaletteEntry` — do not create new palette model.**
- **Story 1.3 (ports):** 9 ABCs in `runtime.ports.*`: `IColorSchemeGenerator.generate(...) -> PaletteEntry`, `IEffectsGenerator.generate(...) -> EffectsEntry`, `IIconRenderer.render(...) -> IconsEntry`, `IStaticWallpaperBackend`, `IVideoWallpaperBackend`, `IWallpaperBackendFactory(auto_detect->BackendType|None)`, `IDesktopConfigWriter`, `IDesktopReloader`, `IStateRepository(load_current/save)`. Each `ABC+@abstractmethod` imports only `runtime.domain.models` + stdlib. Review patches: `FitMode` enum, `Literal` constraints, `auto_detect|None`. **Update port signature to `Path`-based if needed, keep ABC.**
- **Story 1.4 (CSG determinism):** harness at `src/runtime/tests/integration/test_csg_determinism.py` proves `csg generate` deterministic via double-run with literal env `COLORSCHEME__OUTPUT__DIRECTORY=<out>` + `COLORSCHEME__OUTPUT__OVERWRITE=true`, binary `read_bytes`+`sha256`, artifact `tests/integration/.csg_determinism.json` (`deterministic:true`, `wallpaper_hash 619cd350...`, `template_hash dab5e116...`). Option A confirmed (deterministic, no seed). `NondeterministicCSGError(AssertionError)` defined. **Crucial precedent:** test uses **zero `-o` flag**, only env override, and asserts host `tmp/out` receives files even when `runtime.mode==container` — this is the container-forwarding proof pattern this story's adapter must satisfy (env passthrough, not just host process).
- **Story 1.5 (hashing):** `src/runtime/src/runtime/adapters/hashing.py` with `HASH_ALGORITHM: Final[Literal["sha256"]]="sha256"`, chunked `hash_file` (64 KiB, binary), `canonical_hash_dir` (sorted `rel:hash` joined by `\x00`, filtered `__pycache__`/`*.pyc`/`.git`, per-file `OSError→rel:unreadable`, symlink-aware, empty dir → `e3b0c...b855`), `palette/effects/icons_entry_hash` with fixed 64-char hex concatenation + `seed=None` seam + `_validate_hex64`, `hash_file_bytes` helper. 19 unit tests in `tests/unit/test_hashing.py`; layering 37 passed. **Reuse ALL hashing helpers — do NOT reimplement `hashlib.sha256` inline.**
- **Story 1.6 (cache populator):** `src/runtime/src/runtime/adapters/cache.py` with `HASH_ALGORITHM` guard, `CACHE_LAYERS`, `cache_entry_path(state_root, layer, hash)`, `populate_via_staging(target, populate_fn) -> bool` (sibling `cache/.staging-<pid>-<uuid.hex>`, `os.rename` atomic, discard if exists, `FileExistsError`/`ENOTEMPTY`/`EEXIST`/`EISDIR` TOCTOU handling, `try/finally` cleanup, validation `..` + `cache/<layer>/<hash>` depth), `hardlink_or_copy(src,dst)` (`os.link` + `EXDEV→shutil.copy2` fallback), 18 unit tests in `tests/unit/test_cache.py`, 78 tests green (19 hashing + 18 cache + 37 layering + 4 determinism via `-q`), `ruff`/`mypy` clean. **Future composition:** `populate_via_staging(cache_entry_path(state_root,"palettes",ph), lambda staging: CsgAdapter().generate(wallpaper_path, staging))` — adapter MUST write to the `staging` Path given, not create its own staging. Review findings (D1-D3/P1-P8/W1-W2) already patched: empty-staging guard, orphan sweep, `src.is_file()` check, `HASH_ALGORITHM` guard, `uuid4().hex` full, TOCTOU narrow, etc. — carry these guarantees.
- **What NOT to reuse yet:** Do NOT add `IEffectsGenerator`/`IIconRenderer` adapter concrete classes (arrive 1.8/1.9), do NOT add `IStateRepository` JSON adapter (1.10), do NOT add CLI or use cases. This story is csg-adapter only — keep scope to `IColorSchemeGenerator` env override.
- **Review learnings from 1.6:** `HASH_ALGORITHM` guard must be `if HASH_ALGORITHM != "sha256": raise AssertionError` (not `assert` — stripped under `python -O`); staging `mkdir` inside `try`; `uuid4().hex` full (not `[:8]`); TOCTOU must handle `ENOTEMPTY`/`EEXIST`/`EISDIR` plus `errno is None and "File exists" in str(e)` narrow; `_validate_target` must reject `..` + shallow paths + non-hex; `hardlink_or_copy` needs `src.is_file()` guard; `meta.json` guard must exist before `os.rename`. Carry these defensive patterns into csg adapter (e.g., `output_dir` validation via `cache_entry_path`-style check if adapter validates palette path).

### Git intelligence — recent work patterns

- Last commits: `054504e fix: auto-commit code review findings`, `b32daa3 feat(runtime): layered cache populator with staging-dir (rt-1-6)`, `3110bf3 chore: checkpoint before rt-1-6 (baseline c210966)`, `c210966 fix: auto-commit code review findings`, `e03a73e feat: auto-commit story implementation - rt-1-4 csg determinism verification`, `6c64f94 feat(runtime): implement 9 port ABCs`. Each story commits `feat(runtime): ...` with `baseline_commit` captured in frontmatter (this story's baseline is `054504e` or `b32daa3` — capture actual HEAD at dev start).
- Conventions: `tool.ruff` (target-version py314, line-length 100, quote-style double), `tool.mypy strict=true`, `tool.pytest.ini_options`. New code must comply with `ruff format` (don't create new E501/B008 beyond existing ignores in `cli/main.py`/`domain/models.py`); `adapters/csg_adapter.py` must be `ruff check` + `ruff format --check` clean.
- `src/runtime/src/runtime/adapters/__init__.py` remains empty/minimal — this story adds `adapters/csg_adapter.py`; keep `__init__.py` clean (no star imports, no cross-package re-exports).
- `uv run --directory src/runtime pytest -q` runs full suite (60+ before, 78 after 1.6); `pytest -k csg_adapter -v` isolates new tests. `src/runtime/tests/integration/.csg_determinism.json` is `.gitignore`'d; do not commit it; new integration test must not commit new dot-artifacts — write only to `tmp_path`.
- `sprint-status.yaml` story location: `/_bmad-output/implementation-artifacts` — this story's key `rt-1-7-csg-adapter-env-override` must be updated to `ready-for-dev` → `in-progress` → `review` → `done` via workflow; `sprint-status.yaml` last_updated should be bumped to today when story created.
- Baseline branch for this story: `b32daa3`..`054504e` (post-rt-1-6 review fixes). Capture `baseline_commit: 054504e` in frontmatter (HEAD at story creation).

### Project Structure Notes

- **Alignment (AD-13):** `src/runtime/src/runtime/{domain,ports,adapters,application,cli}` + `tests/architecture/test_layering.py` + `tests/unit`/`tests/integration`. This story adds `adapters/csg_adapter.py` (I/O layer, allowed `os`/`subprocess`/`pathlib`) + `tests/unit/test_csg_adapter.py` (unit, fast path) + optional `tests/integration/test_csg_adapter_integration.py` under `src/runtime/`, matching `testpaths`/`pythonpath`. No new package folder needed.
- **Detected conflicts:** The port `IColorSchemeGenerator.generate` currently declares `(wallpaper_hash: str, template_dir: str, output_dir: str) -> PaletteEntry` (ports/color_scheme_generator.py). The story's AC describes `generate(wallpaper_path: Path, output_dir: Path) -> PaletteEntry` (Path-based, realistic for adapter that needs `hash_file` + `canonical_hash_dir`). This is a minor signature divergence (str vs Path). **Resolution:** Update port ABC to accept `Path` args in this story (preferred: adapter is FS-bound, port should reflect that; `pathlib` is allowed in ports — verify `test_layering.py` doesn't ban it there; domain ban only). If port update is blocked, adapter must accept `Path` externally and bridge to `str` for port compatibility, but keep ABC consistent. Document choice in completion notes — do NOT have two incompatible signatures.
- **Naming:** `adapters/csg_adapter.py` mirrors `adapters/hashing.py` + `adapters/cache.py` snake_case; class `CsgAdapter` implements `IColorSchemeGenerator`; tests `test_csg_adapter.py` mirrors `test_hashing.py`/`test_cache.py`; env key `COLORSCHEME__OUTPUT__DIRECTORY` is literal per `shared-data-contract.md` (double underscores, not single). Staging prefix `.staging-` is not used directly by adapter — only via future `populate_via_staging` composition.
- **Tech stack (pinned):** `python>=3.14`, `typer>=0.12`, `cli-output` (via `uv.sources`), `color-scheme-generator[custom]` → `pillow>=11`, `numpy>=2`, `scikit-learn>=1.6`, `jinja2>=3.1`, `pydantic>=2.0`. Adapter shells out to `csg` binary (installed via `csg` console_scripts, or via container image `csg-pywal-podman:latest`/`csg-pywal-docker:latest` when container mode). `csg` discovery via `shutil.which("csg")` — same as determinism test.
- **Spine vs defaults:** Wallpaper source is any `Path` (user image); `wallpaper_hash = hash_file(path)` gives `wh`. Templates dir is provisioning-owned spine input `src/cli-tools/color-scheme-generator/src/color_scheme_generator/defaults/templates` (READ-ONLY, part of cache key via `canonical_hash_dir`). Adapter reads it for hashing only, never writes. Later `ApplyWallpaperUseCase` will compute `ph` and call `cache_entry_path(state_root,"palettes",ph)` to get `output_dir`, then either `populate_via_staging(... lambda s: adapter.generate(wallpaper_path, s))` or direct `adapter.generate`.
- **State root injection:** Adapter takes `output_dir: Path` injected by caller (future `SeedCacheUseCase`/`ApplyWallpaperUseCase` owns `state_root` discovery via `XDG_STATE_HOME`); adapter never calls `os.environ["XDG_STATE_HOME"]` itself — caller injects. Keeps adapter testable (uses `tmp_path/state` in tests).
- **Container image discovery:** `csg` binary may be host-installed or container-only; adapter's `is_available()` should check both: `shutil.which("csg") is not None` **or** container image exists via `oci-runtime`? But Phase 2 container image check is deferred to `ContainerProcessor` (Story 3.2) — for Story 1.7, `is_available` via `which` suffices. Document that container-mode availability (image exists) is checked later, not here.

### References

- Architecture: `_bmad-output/planning-artifacts/architecture/architecture-dotfiles-repo-v3-2026-08-18/ARCHITECTURE-SPINE.md` — AD-1, AD-2, AD-3, AD-5, AD-6, AD-7 (env override literal keys + container forwarding), AD-8, AD-9, AD-13, AD-14, AD-15, AD-16, Deferred: cache eviction, CSG determinism verification, AGS reload, Hyprpaper channel
- Shared data contract: `_bmad-output/planning-artifacts/architecture/architecture-dotfiles-repo-v3-2026-08-18/shared-data-contract.md` — current.json/history.jsonl/meta.json schemas, `hash_algorithm: sha256`, `input_template_hash`, `artifact_hashes:{colors.yaml,colors.conf,colors.gtk.css}`, Derivation-input hashing (palette = `sha256(wallpaper_hash || template_set_hash)`), Env-override protocol literal keys (`COLORSCHEME__OUTPUT__DIRECTORY`, `WALLPAPER__OUTPUT__DIRECTORY`, `ICON_RENDERER__OUTPUT__OUTPUT_DIR`/`ICON_RENDERER__COLOR_SCHEME__PATH`), Swap sequence ownership (`ReconcileDesktopStateUseCase`)
- Epics: `_bmad-output/planning-artifacts/epics-dotfiles-runtime-phase2.md` — Epic 1 Story 1.7 ACs (FR-1, CAP-1, AD-7), cross-story deps 1.5→1.6→1.7, correct-course AGS swap already done (1.14), Epic 2/3 later
- SPEC: `_bmad-output/specs/spec-dotfiles-runtime-phase2/SPEC.md` — CAP-1/2, Constraint 3 (FS authority), Constraint 6 (SHA-256), Constraint 8 (env override), Assumption: CSG deterministic (verified Story 1.4 Option A)
- Cache model: `_bmad-output/specs/spec-dotfiles-runtime-phase2/cache-model.md` — layered cache layout (`cache/wallpapers/<wh>`, `cache/palettes/<ph>`, `cache/effects/<eh>`, `cache/icons/<ih>`), derivation layers and cache keys, staging-dir (`cache/.staging-<pid>/` then `os.rename`), hardlink (`wallpaper.png` hardlinked), cache-hit flow, first-run seeding, consumer wiring through `current/` symlinks
- Previous stories: `_bmad-output/implementation-artifacts/rt-1-1-nested-hexagon-scaffold.md`, `rt-1-2-domain-model-derivation-graph.md`, `rt-1-3-ports-domain-capabilities.md`, `rt-1-4-csg-determinism-verification.md`, `rt-1-5-content-hashing-and-cache-key.md`, `rt-1-6-layered-cache-populator.md` (78 tests green, layering 37 passed, hashing cache-model compliant)
- Hashing helpers: `src/runtime/src/runtime/adapters/hashing.py` — `HASH_ALGORITHM`, `hash_file` (chunked 64 KiB, binary), `canonical_hash_dir` (sorted `\x00`-joined, `__pycache__` filter, symlink-aware, `OSError→unreadable` sentinel), `palette_entry_hash` (hex validation, seed seam), `hash_file_bytes`, `_is_hex64`
- Cache populator: `src/runtime/src/runtime/adapters/cache.py` — `HASH_ALGORITHM` guard, `CACHE_LAYERS`, `cache_entry_path`, `populate_via_staging` (sibling staging `cache/.staging-<pid>-<uuid>`, `os.rename` atomic, discard race, cleanup, `..` validation), `hardlink_or_copy` (`os.link` + `EXDEV→copy2`), `CACHE_STAGING_PREFIX`
- Determinism harness: `src/runtime/tests/integration/test_csg_determinism.py` — double-run `csg generate` via `COLORSCHEME__OUTPUT__DIRECTORY` + `OVERWRITE=true`, zero `-o` flag (container forwarding proof), `NondeterministicCSGError`, `.csg_determinism.json` artifact (`deterministic:true`), `_run_csg_generate` helper (literal env keys, `--format yaml/conf/gtk.css`, `capture_output`, `timeout=120`), `_find_csg_templates_dir` (repo search), `_normalize_yaml_bytes` (strip `generated_at`)
- Domain models: `src/runtime/src/runtime/domain/models.py` — `WallpaperEntry`, `PaletteEntry(artifact_hashes:PaletteArtifacts)`, `EffectsEntry`, `IconsEntry`, `BackendType`, `FitMode`, `DesktopState(schema_version:Literal[2])`
- Ports: `src/runtime/src/runtime/ports/color_scheme_generator.py` — `IColorSchemeGenerator.generate(...) -> PaletteEntry` (ABC, will be updated to Path-based if needed; verify `test_layering.py` allowlist for ports), sibling `effects_generator.py`/`icon_renderer.py` (for Stories 1.8–1.9)
- Layering test: `src/runtime/tests/architecture/test_layering.py` — allowlist (`dataclasses`,`enum`,`typing`,`collections`,`collections.abc`,`functools`,`re`,`__future__`), banned `subprocess`/`os`/`shutil`/`pathlib`/`io` in domain, `ports` are ABCs, cross-package forbidden set (`provisioning`, `core`, `infrastructure`, `color_scheme_generator`, ...), `domain` has no Path FS calls
- Sprint status: `_bmad-output/implementation-artifacts/sprint-status.yaml` — `rt-1-7-csg-adapter-env-override: backlog` (next), `rt-1-6: done/review` → now `rt-1-7` ready-for-dev
- Git: baseline `054504e` — post-rt-1-6 review fixes (D1-D3/P1-P8); `src/runtime/pyproject.toml` — `tool.ruff` (py314, line 100, double quotes), `tool.mypy strict=true`, `requires-python>=3.14`
- CSG defaults/templates (spine READ-ONLY): `src/cli-tools/color-scheme-generator/src/color_scheme_generator/defaults/templates/` (9 templates for `cs` generation), `src/cli-tools/color-scheme-generator/src/color_scheme_generator/defaults/settings.toml` — never edited by runtime

## Dev Agent Record

### Agent Model Used

muse-spark-1.2-contributor-free (opencode/muse-spark-1.2-contributor-free)

### Debug Log References

- Implementation followed red-green: wrote unit tests first, confirmed fail before code, then minimal impl.
- Verified literal env keys: `COLORSCHEME__OUTPUT__DIRECTORY` + `COLORSCHEME__OUTPUT__OVERWRITE` — no single-underscore variant.
- Verified no `-o` flag in subprocess args — container forwarding proof.
- Tested timeout via mocked `subprocess.TimeoutExpired` → `TimeoutError` with "timed out".
- Tested error preservation: non-zero exit raises `RuntimeError` with stderr truncated to 2 KiB.

### Completion Notes List

- ✅ Implemented `src/runtime/src/runtime/adapters/csg_adapter.py` as `CsgAdapter(IColorSchemeGenerator)` per Task 1 — chosen filename `csg_adapter.py` (not `csg.py`) to avoid shadowing external `color_scheme_generator` package; documented in docstring.
- ✅ Updated `src/runtime/src/runtime/ports/color_scheme_generator.py` from str-based `(wallpaper_hash, template_dir, output_dir)` to Path-based `(wallpaper_path: Path, output_dir: Path)` per Dev Notes detected conflict resolution — port remains ABC, pathlib allowed in ports layer, domain stays pure.
- ✅ Implemented `generate` with Option A: computes `ph = palette_entry_hash(wallpaper_hash, template_set_hash)` via `adapters/hashing` helpers, validates `output_dir.name == ph`, builds env via `dict(os.environ)` without global mutation, runs `subprocess.run` with `timeout=60`, verifies three artifacts exist, computes `artifact_hashes` via `hash_file` binary chunked, returns `PaletteEntry` with `hash_algorithm==HASH_ALGORITHM==sha256` and `generated_at` UTC ISO.
- ✅ Container forwarding: adapter only sets host env dict; documented that `csg`'s `container_processor` forwards into `RunConfig` — no Docker `-e` manipulation.
- ✅ Error handling: `FileNotFoundError("csg not on PATH")` on missing binary, `RuntimeError` on non-zero exit with stderr/stdout truncation, `TimeoutError` on `TimeoutExpired` with "timed out" substring, early `FileNotFoundError`/`IsADirectoryError` for wallpaper, `ValueError` on entry-hash mismatch.
- ✅ No `settings.toml` edit — never opens settings file; verified via mtime tests.
- ✅ Artifact hashes use `PaletteArtifacts` underscored keys (`colors_yaml`, `colors_conf`, `colors_gtk_css`) per `domain/models.py` — note: story describes dotted keys `colors.yaml` but domain TypedDict uses underscored identifiers; adapter returns underscored to satisfy `mypy --strict` while still being deterministic SHA via `hash_file`.
- ✅ Created 10 unit tests in `src/runtime/tests/unit/test_csg_adapter.py` covering env override, no settings.toml touch, container passthrough, non-zero exit, timeout, missing wallpaper, hash mismatch, hex64, is_available, container no-rewrite — all mock `subprocess.run`, fast path (`pytest -k "not integration"`).
- ✅ Created integration test `src/runtime/tests/integration/test_csg_adapter_integration.py` that calls real `csg` binary (skips if not on PATH) and verifies three files land in `output_dir` and hashes match — proves container forwarding when engine present.
- ✅ Layering verified: `domain` stays pure (allowlist), `ports` ABC only, `adapters` only place with `os/subprocess/pathlib`, no cross-package imports (`provisioning`, `color_scheme_generator` etc.), `ruff check` + `ruff format --check` + `mypy --strict` + `pytest tests/architecture/test_layering.py` all green (39 checks).
- ✅ Validations: `uv run --directory src/runtime pytest -q` → 90 passed (including determinism double-run 44s), `pytest -k "not integration"` → 87 passed, `pytest tests/unit/test_csg_adapter.py -v` → 10 passed.

### File List

- src/runtime/src/runtime/adapters/csg_adapter.py (CREATE) — CsgAdapter with env override, timeout, error handling, templates_dir injection
- src/runtime/src/runtime/ports/color_scheme_generator.py (MODIFY) — Path-based generate signature
- src/runtime/tests/unit/test_csg_adapter.py (CREATE) — 10 unit tests for AC 1-5
- src/runtime/tests/integration/test_csg_adapter_integration.py (CREATE) — integration with real csg, skipped if missing
- _bmad-output/implementation-artifacts/sprint-status.yaml (MODIFY) — status ready-for-dev → in-progress → review
- _bmad-output/implementation-artifacts/rt-1-7-csg-adapter-env-override.md (MODIFY) — Task checkboxes, Status, Dev Record

### Change Log

- 2026-08-29: Implemented CsgAdapter env override, updated port to Path signature, added 10 unit + 1 integration tests, verified layering + ruff + mypy green. Story status → review.

