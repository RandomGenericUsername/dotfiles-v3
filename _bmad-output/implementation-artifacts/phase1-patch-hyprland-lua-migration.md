---
baseline_commit: 87728fc
---

# Phase 1 Patch: Hyprland Lua Migration & Must-Have Utilities

Status: done

## Change Log

- 2026-08-22: Patch applied — migrated hyprland.conf to hyprland.lua with modular structure; added must-have utilities from Hyprland wiki; wired services via systemd.

## Context

Hyprland 0.55+ deprecated hyprlang format (`hyprland.conf`) in favor of Lua (`hyprland.lua`). The deprecation warning appears at boot. Additionally, the Hyprland wiki recommends several must-have utilities that were missing from the provisioning.

## Changes Applied

### Config Migration

| Change | Files |
|--------|-------|
| Migrate `hyprland.conf` → `hyprland.lua` | `dotfiles/config/hypr/hyprland.lua` (new) |
| Add 9 modular Lua config files | `keybindings.lua`, `monitors.lua`, `autostart.lua`, `window-rules.lua`, `animations.lua`, `input.lua`, `decoration.lua`, `cursor.lua`, `env-variables.lua` |
| Remove old `hyprland.conf` | `dotfiles/config/hypr/hyprland.conf` (deleted) |

### Package Additions

| Package | Purpose |
|---------|---------|
| dunst | Notification daemon |
| hyprpolkitagent | Polkit authentication agent |
| xdg-desktop-portal-hyprland | XDG Desktop Portal backend (XDPH) |
| xdg-desktop-portal-gtk | File picker for XDPH (XDPH lacks one per wiki) |
| wl-clipboard | Wayland clipboard utilities |
| grim | Screenshot utility |
| slurp | Region selection for screenshots |
| wofi | Application launcher |
| thunar | File manager (GTK-based) |
| cliphist | Clipboard history |
| lf | Terminal file manager |
| hyprcursor | Cursor themes |
| hyprlock | Screen locker |
| hypridle | Idle management |

### Service Wiring

All services wired via `systemctl --user start` in `autostart.lua`:

| Service | Method |
|---------|--------|
| hyprpolkitagent | `systemctl --user start hyprpolkitagent` |
| xdg-desktop-portal-hyprland | `systemctl --user start xdg-desktop-portal-hyprland` |
| xdg-desktop-portal-gtk | `systemctl --user start xdg-desktop-portal-gtk` |

### Ansible Role Updates

- `compositor_configs/vars/main.yml` — 13 skeleton files (was 4)
- `compositor_configs/tasks/main.yml` — updated comment
- `verify/vars/main.yml` — verify_compositor_skeleton_files list

### Test Updates

- `test_compositor_configs_role.py` — updated expected skeleton count and file list
- `test_compositor_skeleton_configs.py` — updated to reference hyprland.lua
- `test_verify_role.py` — updated to reference hyprland.lua

## Architecture Decisions

1. **Modular Lua structure** — main `hyprland.lua` uses `source = "{{ compositor_configs_xdg_config_home }}/hypr/<module>.lua"` to include each module. The template variable resolves XDG_CONFIG_HOME at provisioning time.

2. **colors.conf stays as-is** — generated by `csg generate -f conf` in hyprlang format. Lua's `source` can include `.conf` files.

3. **Systemd user services** — all services wired via `systemctl --user start` for portability across distros.

4. **xdg-desktop-portal-gtk added** — XDPH doesn't implement a file picker per the Hyprland wiki; xdg-desktop-portal-gtk fills this gap.

5. **No provisioning infrastructure changes** — Ansible roles, paths, and playbook structure unchanged; only content/structure of config files modified.

## Commits

- `e00a677` — feat(hyprland): migrate from hyprlang to Lua format
- `87728fc` — fix(hyprland): wire XDPH via systemd, add xdg-desktop-portal-gtk
- `cba0f98` — docs: document Phase 1 Hyprland Lua migration patch

## Test Results

All 34 compositor-related tests pass after migration.
