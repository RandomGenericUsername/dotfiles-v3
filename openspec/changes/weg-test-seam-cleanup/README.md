# weg-test-seam-cleanup

Replace WEG's env-var/module-global test seam with the CSG-native build_deps monkeypatch pattern; remove test-only machinery from production and enforce robustness with regression guards.
