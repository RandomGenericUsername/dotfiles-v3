## 1. Add help text to info command

- [x] 1.1 Add description to `info` — `help=` in `app.command()(info)`

## 2. Add help text to dump-config command

- [x] 2.1 Add description to `dump_config` — `help=` in registration

## 3. Add help text to dump-templates command

- [x] 3.1 Add description to `dump_templates` — `help=` in registration

## 4. Add help text to install command

- [x] 4.1 Add description to `install` — `help=` in registration

## 5. Add help text to uninstall command

- [x] 5.1 Add description to `uninstall` — `help=` in registration

## 6. Add help text to list-backends command

- [x] 6.1 Add description to `list_backends` — `help=` in registration

## 7. Add help text to show command

- [x] 7.1 Add description to `show` — `help=` in registration

## 8. Add help text to version command

- [x] 8.1 Add description to `version` — `help=` in registration

## 9. Add help text to generate command

- [x] 9.1 Add description to `generate` — `help=` in `@app.command()`

## 10. Verify

- [x] 10.1 Run `csg --help` and confirm all 9 commands have descriptions
- [x] 10.2 Run `csg <command> --help` for each command and confirm descriptions appear
- [x] 10.3 Run existing unit tests to check for regressions
