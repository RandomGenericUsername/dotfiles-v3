# cli-output-shared

Introduce a reusable hexagonal `cli-output` shared package (domain-neutral renderable views, a `Renderer` port, and `json`/`plain`/`rich` renderer adapters) that WEG, CSG, and future modules use for output rendering — additive only, no CLI behavior change.
