# icon-templates-renderer Settings
# Project-level configuration. Keys are namespaced per section.
# See docs/ for architecture reference.

[output]
output_dir = "/tmp/icon-templates-renderer"
verbosity = 1

# Templates and color scheme are resolved by discovery when absent.
# [templates]
# dir = "~/.config/itr/templates"

# [color_scheme]
# path = "~/.config/itr/colors.yaml"
