# GUI tools role vars — standalone desktop GUI applications under
# src/gui-tools/<tool>/, each with its own AGS instance, autostart slot, and
# spine config dir (<install>/config/<app>/, symlinked to ~/.config/<app> by
# the config-links role).
#
# Mirror-and-adapt discipline (Epic 1 retro action item) — what differs from
# the compositor_configs mirror:
#   (a) vs compositor_configs (skeletons for the bar/compositor apps):
#       this role places whole APP dirs (one entry per file, same per-file
#       `template` + `force: true` discipline — `copy` with a directory src
#       + a pre-existing dest dir is a silent no-op, review finding
#       2026-08-12). Each app gets its own spine dir; the role never touches
#       the bar's `ags/` tree.
#   (b) vs cli_tools (backend binaries): cli_tools installs the controller
#       binaries to the user bin dir; this role places the AGS UI sources.
#       The two meet at runtime via $PATH + the instance bus, never on disk.

# gui_tools_repo_root: repo root derived from this playbook's location.
#   Mirror of compositor_configs_repo_root / cli_tools_repo_root exactly.
gui_tools_repo_root: "{{ playbook_dir }}/../../../.."

# gui_tools_xdg_state_home: the XDG state home the app log dir lives under
#   ($XDG_STATE_HOME, spec default ~/.local/state). Identical derivation to
#   the sibling roles (compositor_configs_xdg_state_home et al.): honors
#   $XDG_STATE_HOME with the spec default {{ ansible_facts.env.HOME }} +
#   '/.local/state'. Uses ansible_facts.env.* (F4 lock — never the deprecated
#   top-level ansible_env fact).
gui_tools_xdg_state_home: "{{ ansible_facts.env.XDG_STATE_HOME | default(ansible_facts.env.HOME | default(ansible_facts.user_dir) + '/.local/state', true) }}"

# gui_tools_xdg_config_home: the XDG config home the config_links role symlinks
#   each managed app dir into (~/.config/<app> -> <install>/config/<app>).
#   IDENTICAL derivation to gui_tools_xdg_state_home (and the sibling roles):
#   honors $XDG_CONFIG_HOME with the spec default ~/.config. Used to build the
#   always-on instances' `ags run -d` launch commands — the SAME path autostart
#   uses (via the symlink), never the spine path directly.
gui_tools_xdg_config_home: "{{ ansible_facts.env.XDG_CONFIG_HOME | default(ansible_facts.env.HOME | default(ansible_facts.user_dir) + '/.config', true) }}"

# gui_tools_state_dirs: runtime state dirs the apps need beyond the XDG base
#   homes the filesystem role creates. Currently the AGS per-instance log
#   dir consumed by autostart's --log-file. Owned here (not in autostart):
#   ensuring directories is provisioning's responsibility; the login
#   launcher only starts processes.
gui_tools_state_dirs:
  - "{{ gui_tools_xdg_state_home }}/ags"

# gui_tools_spine_config_dir: the config-in-spine destination root
#   (<install>/config/). Mirror of compositor_configs_spine_config_dir.
#   Trim lock: consumes the same validated {{ install_dir | trim }} value.
gui_tools_spine_config_dir: "{{ install_dir | trim }}/config"

# gui_tools_config_dirs: the standalone app dirs under the config-in-spine
#   home. Re-ensured as directories before the copies (copy does NOT create
#   dest parents for file sources) so a direct gui-tools.yaml run is
#   self-contained.
gui_tools_config_dirs:
  - "{{ gui_tools_spine_config_dir }}/ags-capture"
  - "{{ gui_tools_spine_config_dir }}/ags-capture/ui"
  - "{{ gui_tools_spine_config_dir }}/ags-capture/controllers"
  - "{{ gui_tools_spine_config_dir }}/ags-capture/lib"
  - "{{ gui_tools_spine_config_dir }}/ags-icme"
  - "{{ gui_tools_spine_config_dir }}/ags-icme/ui"
  - "{{ gui_tools_spine_config_dir }}/ags-icme/lib"
  - "{{ gui_tools_spine_config_dir }}/ags-hypr-pano"
  - "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/ui"
  - "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/lib"
  - "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector"
  - "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/ui"
  - "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib"
  - "{{ gui_tools_spine_config_dir }}/ags-notifications"
  - "{{ gui_tools_spine_config_dir }}/ags-notifications/ui"

# gui_tools_app_files: the app FILES, one entry per file with the explicit
#   target path. Per-file entries (not directory sources) are REQUIRED (see
#   note above). Rendered via `ansible.builtin.template` with `force: true`
#   (repo-authoritative, mirror of the compositor_configs decision
#   2026-08-26).
gui_tools_app_files:
  - { name: capture-app-tsx, source: src/gui-tools/capture-tool/app.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/app.tsx" }
  - { name: capture-style-css, source: src/gui-tools/capture-tool/style.css, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/style.css" }
  - { name: capture-types, source: src/gui-tools/capture-tool/types.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/types.ts" }
  - { name: capture-window, source: src/gui-tools/capture-tool/ui/CaptureWindow.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/ui/CaptureWindow.tsx" }
  - { name: capture-recording-view, source: src/gui-tools/capture-tool/ui/RecordingView.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/ui/RecordingView.tsx" }
  - { name: capture-screenshot-view, source: src/gui-tools/capture-tool/ui/ScreenshotView.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/ui/ScreenshotView.tsx" }
  - { name: capture-controller, source: src/gui-tools/capture-tool/controllers/CaptureController.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/controllers/CaptureController.ts" }
  - { name: capture-recording-controller, source: src/gui-tools/capture-tool/controllers/RecordingController.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/controllers/RecordingController.ts" }
  - { name: capture-screenshot-controller, source: src/gui-tools/capture-tool/controllers/ScreenshotController.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/controllers/ScreenshotController.ts" }
  - { name: capture-target-resolver, source: src/gui-tools/capture-tool/controllers/TargetResolver.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/controllers/TargetResolver.ts" }
  # The capture instance resolves palette-aware icons through its own
  # registry (hypr-pano pattern: each GUI app owns its lib/ in src and the
  # role deploys it per-file). The capture registry prefers the instance's
  # own manifest (`../icons.json` → the ags-capture manifest the
  # compositor_configs role generates into the same spine dir) and falls back
  # to the shared bar manifest. Repo-authoritative, force: true.
  - { name: capture-lib-icon-registry, source: src/gui-tools/capture-tool/lib/icon-registry.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-capture/lib/icon-registry.ts" }

# gui_tools_icme_app_files: the icon color mapping editor's app files,
#   deployed into their own ags-icme/ spine dir. RAW COPY (ansible.builtin.copy),
#   NOT `template`: the editor sources contain literal `{{PLACEHOLDER}}`
#   sequences (SVG placeholder regexes and docs) that Jinja2 would evaluate
#   and destroy. Repo-authoritative (force: true) like the capture app — the
#   editor's CODE converges; the DATA it edits (icon-mappings) is seed-once
#   in the assets role.
gui_tools_icme_app_files:
  - { name: icme-app-tsx, source: src/gui-tools/icon-color-mapping-editor/app.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/app.tsx" }
  - { name: icme-style-css, source: src/gui-tools/icon-color-mapping-editor/style.css, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/style.css" }
  - { name: icme-editor-window, source: src/gui-tools/icon-color-mapping-editor/ui/EditorWindow.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/EditorWindow.tsx" }
  - { name: icme-templates-tab, source: src/gui-tools/icon-color-mapping-editor/ui/TemplatesTab.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/TemplatesTab.tsx" }
  - { name: icme-preview, source: src/gui-tools/icon-color-mapping-editor/ui/Preview.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/Preview.tsx" }
  - { name: icme-inputs-panel, source: src/gui-tools/icon-color-mapping-editor/ui/InputsPanel.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/InputsPanel.tsx" }
  - { name: icme-group-tree, source: src/gui-tools/icon-color-mapping-editor/ui/GroupTree.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/GroupTree.tsx" }
  - { name: icme-selection-panel, source: src/gui-tools/icon-color-mapping-editor/ui/SelectionPanel.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/SelectionPanel.tsx" }
  - { name: icme-scope-switch, source: src/gui-tools/icon-color-mapping-editor/ui/ScopeSwitch.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/ScopeSwitch.tsx" }
  - { name: icme-token-picker, source: src/gui-tools/icon-color-mapping-editor/ui/TokenPicker.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/TokenPicker.tsx" }
  - { name: icme-diff-pane, source: src/gui-tools/icon-color-mapping-editor/ui/DiffPane.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/ui/DiffPane.tsx" }
  - { name: icme-lib-itr, source: src/gui-tools/icon-color-mapping-editor/lib/itr.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/itr.ts" }
  - { name: icme-lib-model, source: src/gui-tools/icon-color-mapping-editor/lib/model.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/model.ts" }
  - { name: icme-lib-substitute, source: src/gui-tools/icon-color-mapping-editor/lib/substitute.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/substitute.ts" }
  - { name: icme-lib-svg, source: src/gui-tools/icon-color-mapping-editor/lib/svg.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/svg.ts" }
  - { name: icme-lib-inputs, source: src/gui-tools/icon-color-mapping-editor/lib/inputs.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/inputs.ts" }
  - { name: icme-lib-diff, source: src/gui-tools/icon-color-mapping-editor/lib/diff.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/diff.ts" }
  - { name: icme-lib-templates, source: src/gui-tools/icon-color-mapping-editor/lib/templates.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/templates.ts" }
  - { name: icme-lib-event-contract, source: src/gui-tools/icon-color-mapping-editor/lib/event-contract.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/event-contract.ts" }
  - { name: icme-lib-event-bus-core, source: src/gui-tools/icon-color-mapping-editor/lib/event-bus-core.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/event-bus-core.ts" }
  - { name: icme-lib-event-bus, source: src/gui-tools/icon-color-mapping-editor/lib/event-bus.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-icme/lib/event-bus.ts" }

# gui_tools_hypr_pano_app_files: the clipboard overlay's app files, deployed
#   into their own ags-hypr-pano/ spine dir. Rendered via `template` with
#   force: true (repo-authoritative); the sources contain no Jinja-sensitive
#   `{{`/`{%` sequences (verified by the provisioning role test).
gui_tools_hypr_pano_app_files:
  - { name: hypr-pano-app-tsx, source: src/gui-tools/hypr-pano/app.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/app.tsx" }
  - { name: hypr-pano-style-css, source: src/gui-tools/hypr-pano/style.css, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/style.css" }
  - { name: hypr-pano-window, source: src/gui-tools/hypr-pano/ui/PanoWindow.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/ui/PanoWindow.tsx" }
  - { name: hypr-pano-item-card, source: src/gui-tools/hypr-pano/ui/ItemCard.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/ui/ItemCard.tsx" }
  - { name: hypr-pano-preview, source: src/gui-tools/hypr-pano/ui/Preview.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/ui/Preview.tsx" }
  - { name: hypr-pano-lib-clipboard-types, source: src/gui-tools/hypr-pano/lib/clipboard-types.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/lib/clipboard-types.ts" }
  - { name: hypr-pano-lib-icon-registry, source: src/gui-tools/hypr-pano/lib/icon-registry.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/lib/icon-registry.ts" }
  - { name: hypr-pano-lib-history, source: src/gui-tools/hypr-pano/lib/history.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/lib/history.ts" }
  - { name: hypr-pano-lib-event-bus-core, source: src/gui-tools/hypr-pano/lib/event-bus-core.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/lib/event-bus-core.ts" }
  - { name: hypr-pano-lib-event-bus, source: src/gui-tools/hypr-pano/lib/event-bus.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-hypr-pano/lib/event-bus.ts" }

# gui_tools_wallpaper_selector_app_files: the wallpaper selector's app files,
#   deployed into their own ags-wallpaper-selector/ spine dir. Rendered via
#   `template` with force: true (repo-authoritative); the sources contain no
#   Jinja-sensitive `{{`/`{%` sequences (verified by the provisioning role
#   test) — same placement discipline as the capture app and hypr-pano.
gui_tools_wallpaper_selector_app_files:
  - { name: wallpaper-selector-app-tsx, source: src/gui-tools/wallpaper-selector/app.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/app.tsx" }
  - { name: wallpaper-selector-style-css, source: src/gui-tools/wallpaper-selector/style.css, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/style.css" }
  - { name: wallpaper-selector-window, source: src/gui-tools/wallpaper-selector/ui/WallpaperSelectorWindow.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/ui/WallpaperSelectorWindow.tsx" }
  - { name: wallpaper-selector-lib-model, source: src/gui-tools/wallpaper-selector/lib/model.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/model.ts" }
  - { name: wallpaper-selector-lib-scan, source: src/gui-tools/wallpaper-selector/lib/scan.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/scan.ts" }
  - { name: wallpaper-selector-lib-thumbnails, source: src/gui-tools/wallpaper-selector/lib/thumbnails.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/thumbnails.ts" }
  - { name: wallpaper-selector-lib-apply, source: src/gui-tools/wallpaper-selector/lib/apply.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/apply.ts" }
  - { name: wallpaper-selector-lib-contrast, source: src/gui-tools/wallpaper-selector/lib/contrast.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/contrast.ts" }
  - { name: wallpaper-selector-lib-icon-registry, source: src/gui-tools/wallpaper-selector/lib/icon-registry.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/icon-registry.ts" }
  - { name: wallpaper-selector-lib-event-bus-core, source: src/gui-tools/wallpaper-selector/lib/event-bus-core.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/event-bus-core.ts" }
  - { name: wallpaper-selector-lib-event-bus, source: src/gui-tools/wallpaper-selector/lib/event-bus.ts, dest: "{{ gui_tools_spine_config_dir }}/ags-wallpaper-selector/lib/event-bus.ts" }

# gui_tools_notifications_app_files: the notifd overlay's app files,
#   deployed into their own ags-notifications/ spine dir. Rendered via
#   `template` with force: true (repo-authoritative); the sources contain no
#   Jinja-sensitive `{{`/`{%` sequences (verified by the provisioning role
#   test) — same placement discipline as hypr-pano and the wallpaper
#   selector. The overlay renders the daemon-provided image paths directly,
#   so it owns no icon registry of its own (the EMITTER resolves
#   current/icons/ paths in the capture backend).
gui_tools_notifications_app_files:
  - { name: notifications-app-tsx, source: src/gui-tools/notifications/app.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-notifications/app.tsx" }
  - { name: notifications-style-css, source: src/gui-tools/notifications/style.css, dest: "{{ gui_tools_spine_config_dir }}/ags-notifications/style.css" }
  - { name: notifications-window, source: src/gui-tools/notifications/ui/NotificationsWindow.tsx, dest: "{{ gui_tools_spine_config_dir }}/ags-notifications/ui/NotificationsWindow.tsx" }
  - { name: notifications-makefile, source: src/gui-tools/notifications/Makefile, dest: "{{ gui_tools_spine_config_dir }}/ags-notifications/Makefile" }

# gui_tools_ags_instances: the AGS process instances this role manages
#   (names as reported by `ags list`). AGS bundles TypeScript at startup and
#   holds the compiled bundle in memory; without a restart the running process
#   keeps serving the old code even after provisioning has overwritten the
#   sources on disk — so every entry is quit after placement. Each entry is a
#   mapping:
#     name      — the instance name on the AGS bus (`ags quit -i <name>`).
#     always_on — true for the session-critical instances the machine must
#                 KEEP running (the status bar `ags`, the notification overlay
#                 `notifications`); false for the keybind-launched tools
#                 (`capture`, `icon-color-mapping-editor`, `hypr-pano`,
#                 `wallpaper-selector`), which provisioning deliberately
#                 leaves quit (§ per-instance lifecycle below).
#     launch    — for always_on entries ONLY: the exact `ags run` command this
#                 role re-issues when the instance is not on the bus (mirror of
#                 the autostart.lua line). On-demand entries carry no launch —
#                 their provisioned keybind launchers (cli_tools) start them.
#   Single source of truth: "which instances are always on" is exactly the
#   always_on flag here (and its derived view below) — never a second list.
#
#   Lifecycle split (owner intent, provisioning-keeps-ags-instances-running):
#   - always_on: quit to re-bundle, then brought back UP on the spot (detached
#     start-if-down below) so a provision run never leaves the session without
#     its status bar or — worse — without any owner of
#     org.freedesktop.Notifications (dunst is masked on purpose, so the overlay
#     is the session's only notification daemon).
#   - on-demand: quit and left down; the provisioned keybind launchers
#     (capture-ui, icon-color-mapping-editor, hypr-pano-ui,
#     wallpaper-selector-ui in cli_tools) start them when the user asks, so
#     provisioning never opens a window nobody requested.
gui_tools_ags_instances:
  - name: ags
    always_on: true
    launch: "ags run"
  - name: capture
    always_on: false
  - name: icon-color-mapping-editor
    always_on: false
  - name: hypr-pano
    always_on: false
  - name: wallpaper-selector
    always_on: false
  - name: notifications
    always_on: true
    launch: "ags run -d {{ gui_tools_xdg_config_home }}/ags-notifications --log-file {{ gui_tools_xdg_state_home }}/ags/notifications.log"

# gui_tools_ags_always_on_instances: DERIVED view of the always_on entries
#   (the subset the role restarts after the quit loop). Derived, never a
#   hand-maintained list, so the always-on set has exactly one source of truth
#   in gui_tools_ags_instances.
gui_tools_ags_always_on_instances: "{{ gui_tools_ags_instances | selectattr('always_on') | list }}"
